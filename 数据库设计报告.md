# 超市进销存管理系统 — 数据库设计报告

> **版本**: v1.0  
> **日期**: 2026-06-05  
> **数据库**: MySQL 8.0.44  
> **数据库名**: `supermarket_db`  
> **字符集**: utf8mb4 / utf8mb4_0900_ai_ci  

---

## 目录

1. [概述](#1-概述)
2. [数据库环境说明](#2-数据库环境说明)
3. [ER 图与表关系](#3-er-图与表关系)
4. [数据表结构设计](#4-数据表结构设计)
   - [4.1 系统用户表 `sys_user`](#41-系统用户表-sys_user)
   - [4.2 供应商表 `supplier`](#42-供应商表-supplier)
   - [4.3 商品表 `goods`](#43-商品表-goods)
   - [4.4 会员表 `member`](#44-会员表-member)
   - [4.5 采购主表 `purchase_main`](#45-采购主表-purchase_main)
   - [4.6 采购明细表 `purchase_detail`](#46-采购明细表-purchase_detail)
5. [索引设计](#5-索引设计)
6. [外键约束设计](#6-外键约束设计)
7. [数据字典汇总](#7-数据字典汇总)
8. [安全性设计](#8-安全性设计)
9. [初始化数据脚本](#9-初始化数据脚本)

---

## 1. 概述

### 1.1 项目背景

超市进销存管理系统是一个基于 B/S 架构的 Web 应用，用于超市日常的商品进货、销售、库存管理。系统分为管理员和普通用户两种角色，涵盖供应商管理、商品管理、员工管理、采购管理、会员管理五大业务模块。

### 1.2 设计目标

- **数据完整性**: 通过主键、外键、唯一约束确保数据一致性
- **可扩展性**: 主键采用自增 BIGINT，预留足够扩展空间
- **查询性能**: 合理设计索引，外键列建立索引加速 JOIN 查询
- **级联删除**: 关键外键设置 ON DELETE CASCADE，保证数据引用完整性

### 1.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 表名 | 小写 + 下划线 | `sys_user`, `purchase_main` |
| 列名 | 小写 + 下划线 | `supplier_code`, `create_time` |
| 主键 | `id` | `id BIGINT AUTO_INCREMENT` |
| 外键 | 关联表名_id | `supplier_id`, `user_id` |
| 索引 | 表名_ibfk_N | `goods_ibfk_1` |
| 唯一约束 | 列名 | `UNIQUE KEY goods_code` |

---

## 2. 数据库环境说明

| 项目 | 说明 |
|------|------|
| 数据库系统 | MySQL 8.0.44 |
| 数据库名称 | `supermarket_db` |
| 默认字符集 | utf8mb4 |
| 默认排序规则 | utf8mb4_0900_ai_ci |
| 存储引擎 | InnoDB（默认） |
| 连接用户 | root |
| 连接地址 | localhost:3306 |
| 时区设置 | Asia/Shanghai |

---

## 3. ER 图与表关系

### 3.1 实体关系说明

```
                    ┌──────────────┐
                    │   supplier   │
                    │  (供应商表)   │
                    └──────┬───────┘
                           │ 1
                           │
                           │ N
                    ┌──────▼───────┐         ┌────────────────┐
                    │    goods     │         │  purchase_main  │
                    │  (商品表)    │         │  (采购主表)      │
                    └──────┬───────┘         └───────┬──────────┘
                           │ 1                       │ 1
                           │                         │
                           │ N            ┌──────────┤
                    ┌──────▼───────┐     │          │ N
                    │purchase_detail│◄───┘   ┌──────▼───────┐
                    │ (采购明细表)  │         │   sys_user   │
                    └──────────────┘         │ (系统用户表)  │
                                             └──────────────┘

                    ┌──────────────┐
                    │   member     │
                    │  (会员表)    │  (独立表, 无外键关联)
                    └──────────────┘
```

### 3.2 表间关系矩阵

| 主表 | 从表 | 外键列 | 关系类型 | 级联删除 |
|------|------|--------|----------|----------|
| `supplier` | `goods` | `supplier_id` | 一对多 | ✅ CASCADE |
| `goods` | `purchase_detail` | `goods_id` | 一对多 | ✅ CASCADE |
| `sys_user` | `purchase_main` | `user_id` | 一对多 | ✅ CASCADE |
| `purchase_main` | `purchase_detail` | `purchase_no` (逻辑关联) | 一对多 | ❌ (通过 purchase_no 逻辑关联) |

---

## 4. 数据表结构设计

### 4.1 系统用户表 `sys_user`

**用途**: 存储系统所有用户（管理员 + 普通员工）的基本信息，同时作为员工信息表使用。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 用户唯一标识 |
| 2 | `username` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 登录账号（管理员6位，普通用户9位） |
| 3 | `password` | VARCHAR(100) | NOT NULL | — | — | 登录密码（明文存储） |
| 4 | `real_name` | VARCHAR(50) | NULL | NULL | — | 真实姓名 |
| 5 | `phone` | VARCHAR(20) | NULL | NULL | — | 电话号码（格式: `区号\|号码`，如 `+86\|13800138001`） |
| 6 | `salary` | DECIMAL(10,2) | NULL | NULL | — | 员工工资 |
| 7 | `role` | INT | NULL | NULL | — | 角色：1=管理员，0=普通用户 |
| 8 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |
| 9 | `create_time` | DATETIME | NULL | CURRENT_TIMESTAMP | — | 创建时间 |

**创建语句（精简）**:
```sql
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(100) NOT NULL,
  `real_name` varchar(50) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `salary` decimal(10,2) DEFAULT NULL,
  `role` int DEFAULT NULL COMMENT '1管理员 0普通用户',
  `remark` varchar(500) DEFAULT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

**业务规则**:
- 系统最多 3 个管理员，账号前缀分别为 `11`、`10`、`01`，各 6 位
- 普通员工账号 9 位，必须以 `00` 开头
- 密码至少 8 位，必须包含大小写字母 + 数字 + 特殊字符（`@$!%*?&`）
- 同一区号下电话号码不能重复
- 所有用户密码不可重复
- 11 开头 = 一号管理员（最高权限），10 开头 = 二号管理员，01 开头 = 三号管理员

---

### 4.2 供应商表 `supplier`

**用途**: 存储供应商基本信息。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 供应商唯一标识 |
| 2 | `supplier_code` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 供应商编码 |
| 3 | `supplier_name` | VARCHAR(100) | NOT NULL | — | — | 供应商名称 |
| 4 | `short_name` | VARCHAR(50) | NULL | NULL | — | 供应商简称 |
| 5 | `address` | VARCHAR(200) | NULL | NULL | — | 公司地址 |
| 6 | `phone` | VARCHAR(20) | NULL | NULL | — | 公司电话 |
| 7 | `email` | VARCHAR(50) | NULL | NULL | — | 公司邮箱 |
| 8 | `contact_person` | VARCHAR(50) | NULL | NULL | — | 联系人姓名 |
| 9 | `contact_phone` | VARCHAR(20) | NULL | NULL | — | 联系人电话（支持国际区号） |
| 10 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |
| 11 | `create_time` | DATETIME | NULL | CURRENT_TIMESTAMP | — | 创建时间 |

**创建语句（精简）**:
```sql
CREATE TABLE `supplier` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `supplier_code` varchar(50) NOT NULL,
  `supplier_name` varchar(100) NOT NULL,
  `short_name` varchar(50) DEFAULT NULL,
  `address` varchar(200) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `email` varchar(50) DEFAULT NULL,
  `contact_person` varchar(50) DEFAULT NULL,
  `contact_phone` varchar(20) DEFAULT NULL,
  `remark` varchar(500) DEFAULT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `supplier_code` (`supplier_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

---

### 4.3 商品表 `goods`

**用途**: 存储商品基本信息，关联供应商。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 商品唯一标识 |
| 2 | `goods_code` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 商品编号 |
| 3 | `goods_name` | VARCHAR(100) | NOT NULL | — | — | 商品名称 |
| 4 | `price` | DECIMAL(10,2) | NULL | NULL | — | 商品单价 |
| 5 | `supplier_id` | BIGINT | NULL | NULL | **FK** → supplier(id) | 供应商ID |
| 6 | `intro` | VARCHAR(500) | NULL | NULL | — | 商品简介 |
| 7 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |
| 8 | `create_time` | DATETIME | NULL | CURRENT_TIMESTAMP | — | 创建时间 |

**创建语句（精简）**:
```sql
CREATE TABLE `goods` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `goods_code` varchar(50) NOT NULL,
  `goods_name` varchar(100) NOT NULL,
  `price` decimal(10,2) DEFAULT NULL,
  `supplier_id` bigint DEFAULT NULL,
  `intro` varchar(500) DEFAULT NULL,
  `remark` varchar(500) DEFAULT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `goods_code` (`goods_code`),
  KEY `goods_ibfk_1` (`supplier_id`),
  CONSTRAINT `goods_ibfk_1` FOREIGN KEY (`supplier_id`) REFERENCES `supplier` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

**关联查询**: 商品查询时 LEFT JOIN `supplier` 表获取 `supplier_name` 字段。

---

### 4.4 会员表 `member`

**用途**: 存储超市会员基本信息，独立表，无外键关联。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 会员唯一标识 |
| 2 | `member_no` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 会员编号 |
| 3 | `name` | VARCHAR(100) | NOT NULL | — | — | 会员姓名 |
| 4 | `phone` | VARCHAR(20) | NULL | NULL | — | 会员电话（支持国际区号） |
| 5 | `register_time` | DATETIME | NULL | CURRENT_TIMESTAMP | — | 注册时间 |
| 6 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |

**创建语句（精简）**:
```sql
CREATE TABLE `member` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `member_no` varchar(50) NOT NULL,
  `name` varchar(100) NOT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `register_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `remark` varchar(500) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `member_no` (`member_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

---

### 4.5 采购主表 `purchase_main`

**用途**: 存储采购单据的主体信息，关联操作用户。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 采购主表唯一标识 |
| 2 | `purchase_no` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 采购清单号 |
| 3 | `user_id` | BIGINT | NULL | NULL | **FK** → sys_user(id) | 操作员工ID |
| 4 | `total_num` | INT | NULL | NULL | — | 采购总数量 |
| 5 | `total_price` | DECIMAL(12,2) | NULL | NULL | — | 采购总金额 |
| 6 | `purchase_time` | DATETIME | NULL | NULL | — | 采购时间 |
| 7 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |
| 8 | `create_time` | DATETIME | NULL | CURRENT_TIMESTAMP | — | 创建时间 |

**创建语句（精简）**:
```sql
CREATE TABLE `purchase_main` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `purchase_no` varchar(50) NOT NULL,
  `user_id` bigint DEFAULT NULL,
  `total_num` int DEFAULT NULL,
  `total_price` decimal(12,2) DEFAULT NULL,
  `purchase_time` datetime DEFAULT NULL,
  `remark` varchar(500) DEFAULT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `purchase_no` (`purchase_no`),
  KEY `purchase_main_ibfk_1` (`user_id`),
  CONSTRAINT `purchase_main_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `sys_user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

---

### 4.6 采购明细表 `purchase_detail`

**用途**: 存储采购单中每一项商品的明细信息，关联商品表。

| 序号 | 字段名 | 数据类型 | 是否为空 | 默认值 | 约束 | 说明 |
|------|--------|----------|----------|--------|------|------|
| 1 | `id` | BIGINT | NOT NULL | AUTO | **PK** | 明细唯一标识 |
| 2 | `detail_no` | VARCHAR(50) | NOT NULL | — | **UNIQUE** | 采购明细编号 |
| 3 | `purchase_no` | VARCHAR(50) | NULL | NULL | — | 关联的采购清单号 |
| 4 | `goods_id` | BIGINT | NULL | NULL | **FK** → goods(id) | 商品ID |
| 5 | `goods_num` | INT | NULL | NULL | — | 采购数量 |
| 6 | `goods_price` | DECIMAL(10,2) | NULL | NULL | — | 商品单价 |
| 7 | `total_price` | DECIMAL(12,2) | NULL | NULL | — | 该明细总价 (= goods_num × goods_price) |
| 8 | `remark` | VARCHAR(500) | NULL | NULL | — | 备注信息 |

**创建语句（精简）**:
```sql
CREATE TABLE `purchase_detail` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `detail_no` varchar(50) NOT NULL,
  `purchase_no` varchar(50) DEFAULT NULL,
  `goods_id` bigint DEFAULT NULL,
  `goods_num` int DEFAULT NULL,
  `goods_price` decimal(10,2) DEFAULT NULL,
  `total_price` decimal(12,2) DEFAULT NULL,
  `remark` varchar(500) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `detail_no` (`detail_no`),
  KEY `purchase_detail_ibfk_1` (`goods_id`),
  CONSTRAINT `purchase_detail_ibfk_1` FOREIGN KEY (`goods_id`) REFERENCES `goods` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

---

## 5. 索引设计

### 5.1 主键索引（自动创建）

| 表名 | 索引名 | 列 |
|------|--------|-----|
| sys_user | PRIMARY | id |
| supplier | PRIMARY | id |
| goods | PRIMARY | id |
| member | PRIMARY | id |
| purchase_main | PRIMARY | id |
| purchase_detail | PRIMARY | id |

### 5.2 唯一索引

| 表名 | 索引名 | 列 | 说明 |
|------|--------|-----|------|
| sys_user | username | username | 保证用户名唯一 |
| supplier | supplier_code | supplier_code | 保证供应商编码唯一 |
| goods | goods_code | goods_code | 保证商品编号唯一 |
| member | member_no | member_no | 保证会员编号唯一 |
| purchase_main | purchase_no | purchase_no | 保证采购清单号唯一 |
| purchase_detail | detail_no | detail_no | 保证明细编号唯一 |

### 5.3 普通索引（外键索引）

| 表名 | 索引名 | 列 | 用途 |
|------|--------|-----|------|
| goods | goods_ibfk_1 | supplier_id | 加速商品-供应商关联查询 |
| purchase_main | purchase_main_ibfk_1 | user_id | 加速采购-用户关联查询 |
| purchase_detail | purchase_detail_ibfk_1 | goods_id | 加速明细-商品关联查询 |

---

## 6. 外键约束设计

| 编号 | 从表 | 外键列 | 主表 | 主表列 | 级联删除 | 级联更新 |
|------|------|--------|------|--------|----------|----------|
| FK-01 | goods | supplier_id | supplier | id | CASCADE | — |
| FK-02 | purchase_main | user_id | sys_user | id | CASCADE | — |
| FK-03 | purchase_detail | goods_id | goods | id | CASCADE | — |

**级联删除说明**：
- 删除供应商时，其关联的商品自动删除
- 删除商品时，其关联的采购明细自动删除
- 删除用户时，其关联的采购主表记录自动删除

---

## 7. 数据字典汇总

### 7.1 电话号码格式

系统中 `phone` 列统一使用 `区号|号码` 格式存储，如 `+86|18047651111`。支持的区号列表：

| 地区 | 区号 | 号码位数 |
|------|------|----------|
| 中国大陆 | +86 | 11位（1开头） |
| 中国香港 | +852 | 8位（5/6/9开头） |
| 中国澳门 | +853 | 8位（6开头） |
| 中国台湾 | +886 | 10位（09开头） |
| 日本 | +81 | 10-11位 |
| 韩国 | +82 | 10-11位 |
| 新加坡 | +65 | 8位 |
| 泰国 | +66 | 10位 |
| 马来西亚 | +60 | 10位 |
| 越南 | +84 | 10位 |
| 印度 | +91 | 10位 |
| 阿联酋 | +971 | 9位 |
| 沙特 | +966 | 9位 |
| 印尼 | +62 | 10-12位 |
| 菲律宾 | +63 | 10位 |
| 美国/加拿大 | +1 | 10位 |
| 俄罗斯 | +7 | 10位 |
| 英国 | +44 | 11位 |
| 德国 | +49 | 10-11位 |
| 法国 | +33 | 9位 |
| 意大利 | +39 | 10位 |
| 西班牙 | +34 | 9位 |
| 瑞士 | +41 | 9位 |
| 瑞典 | +46 | 9位 |
| 挪威 | +47 | 8位 |
| 澳大利亚 | +61 | 9位 |
| 新西兰 | +64 | 8-9位 |
| 巴西 | +55 | 11位 |
| 阿根廷 | +54 | 10位 |

### 7.2 角色编码

| 角色值 | 含义 |
|--------|------|
| 1 | 管理员 |
| 0 | 普通用户（员工） |

### 7.3 管理员分级

| 账号前缀 | 级别 | 名称 | 权限范围 |
|----------|------|------|----------|
| 11 | 一级 | 一号管理员 | 供应商管理、商品管理、员工管理、采购管理、会员管理（全部） |
| 10 | 二级 | 二号管理员 | 供应商管理、商品管理、会员管理（查看员工、采购） |
| 01 | 三级 | 三号管理员 | 员工管理、采购管理、会员管理（查看供应商、商品） |

---

## 8. 安全性设计

### 8.1 当前安全措施

| 措施 | 状态 | 说明 |
|------|------|------|
| 唯一约束 | ✅ 已实现 | username, supplier_code, goods_code, member_no, purchase_no, detail_no |
| 外键约束 | ✅ 已实现 | 防止孤儿记录 |
| CORS 配置 | ✅ 已实现 | 允许跨域请求 |
| 密码加密 | ❌ 未实现 | 当前明文存储，**建议后续升级为 BCrypt 加密** |
| HTTPS | ❌ 未实现 | 当前使用 HTTP |
| SQL 注入防护 | ✅ 已实现 | MyBatis 参数化查询 |

### 8.2 建议改进项

1. **密码加密**: 使用 BCrypt 或 Argon2 对密码进行哈希存储
2. **HTTPS**: 生产环境启用 HTTPS 传输
3. **敏感数据加密**: 对手机号等敏感字段考虑加密存储
4. **审计日志**: 增加操作日志表记录关键操作

---

## 9. 初始化数据脚本

### 9.1 数据库创建

```sql
CREATE DATABASE IF NOT EXISTS supermarket_db 
  DEFAULT CHARSET utf8mb4 
  COLLATE utf8mb4_0900_ai_ci;

USE supermarket_db;
```

### 9.2 全量建表语句

参见 [database/_localhost-2026_06_04_20_24_31-dump.sql](database/_localhost-2026_06_04_20_24_31-dump.sql)

### 9.3 示例初始化数据

```sql
-- 插入三个管理员
INSERT INTO sys_user (username, password, real_name, phone, salary, role, remark, create_time) VALUES
('110001', 'Sh123456@', '陈一', '+86|18047651111', 10000.00, 1, '一号管理员，拥有所有权限', NOW()),
('100001', 'Sh1234567@', '张三', '+86|12345678900', 10000.00, 1, '二号管理员，管理供应商、商品', NOW()),
('010001', 'Sh1234567!', '望山', '+86|12345678911', 10000.00, 1, '三号管理员，管理员工、采购', NOW());

-- 插入示例供应商
INSERT INTO supplier (supplier_code, supplier_name, short_name, address, phone, email, contact_person, contact_phone) VALUES
('S001', '北京宏达商贸有限公司', '宏达商贸', '北京市朝阳区建国路88号', '010-88888888', 'hd@example.com', '张三', '+86|13800138001');
```

---

## 附录 A：表大小估算

| 表名 | 单行估算 | 预期数据量 | 预期总大小 |
|------|----------|-----------|------------|
| sys_user | ~250B | < 1,000 | ~250KB |
| supplier | ~600B | < 10,000 | ~6MB |
| goods | ~400B | < 100,000 | ~40MB |
| member | ~300B | < 100,000 | ~30MB |
| purchase_main | ~300B | < 1,000,000 | ~300MB |
| purchase_detail | ~300B | < 10,000,000 | ~3GB |

---

## 附录 B：版本记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-06-05 | 初始版本，包含全部 6 张表设计 | Sun Hao |
