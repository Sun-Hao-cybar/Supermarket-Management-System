# 白色小猫智能体 — 设计文档

> 日期：2026-06-12  
> 状态：已确认  
> 方案：前端 Vue 组件 + 后端 Spring Boot 代理 DeepSeek API（方案B / 混合方案）

---

## 一、需求概述

登录后在页面右下角出现一只白色小猫智能体（参考 `cat-agent.mp4`），用户可以：
- 和小猫对话，询问进销存系统的操作问题
- 拖拽小猫到屏幕任意位置
- 在三种交互模式间切换（灵动 / 安静 / 折叠）

智能回答采用 **混合方案**：本地知识库覆盖 40+ 条预设问答（即时返回），超出范围的问题转发 DeepSeek API。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                    Frontend (Vue 3)                  │
│  ┌───────────────────────────────────────────────┐  │
│  │           CatAgent.vue (主组件)                │  │
│  │  ┌──────────────┐  ┌──────────────────────┐  │  │
│  │  │ CatAvatar     │  │ ChatDialog           │  │  │
│  │  │ (小猫形象)    │  │ (对话面板)            │  │  │
│  │  │ - 视频播放    │  │ - 消息列表            │  │  │
│  │  │ - 拖拽移动    │  │ - 输入框              │  │  │
│  │  │ - 状态动画    │  │ - 快捷问题            │  │  │
│  │  └──────────────┘  └──────────────────────┘  │  │
│  │  ┌──────────────────────────────────────┐    │  │
│  │  │ knowledgeBase.js (本地知识库)         │    │  │
│  │  │ - 预设问答匹配 → 即时返回             │    │  │
│  │  │ - 未匹配 → 转发后端 API               │    │  │
│  │  └──────────────────────────────────────┘    │  │
│  └───────────────────────────────────────────────┘  │
│                         │                            │
└─────────────────────────┼────────────────────────────┘
                          │ HTTP POST /api/agent/chat
┌─────────────────────────┼────────────────────────────┐
│              Backend (Spring Boot)                    │
│  ┌──────────────────────┴─────────────────────────┐  │
│  │            AgentController                      │  │
│  │  POST /api/agent/chat   ← 对话接口               │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │            AgentService                         │  │
│  │  - 调用 DeepSeek API (OpenAI 兼容格式)           │  │
│  │  - System Prompt: 超市进销存系统帮助角色         │  │
│  └────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**关键决策**：
- 小猫组件仅在 `/layout/*` 路由下可见（登录后），Login 页不展示
- 前端知识库负责快速匹配（0 延迟），匹配不到才调用后端 API
- DeepSeek API Key 存在后端 `application.yml` 中
- 用户行为模式设置存 `localStorage`

---

## 三、知识库设计

### 3.1 预设问题覆盖

| 模块 | 预设问题数 | 覆盖内容 |
|------|-----------|---------|
| **员工管理** | 8 个 | 如何添加/编辑/删除员工、权限说明、管理员等级区别、如何录入新员工 |
| **会员管理** | 6 个 | 如何添加/编辑/删除会员、会员等级、积分规则、如何关联会员卡 |
| **商品管理** | 6 个 | 如何添加/编辑/删除商品、库存查看、分类筛选、商品信息字段说明 |
| **采购管理** | 8 个 | 如何创建采购单/采购退货单、审批流程、采购状态说明、如何查询采购记录 |
| **供应商管理** | 5 个 | 如何添加/编辑/删除供应商、供应商关联商品、供应商信息维护 |
| **通用操作** | 7 个 | 如何修改密码、退出登录、权限不足怎么办、导航说明、角色说明、管理员类型 |

共计 **40+ 条预设问答**。

### 3.2 匹配策略

```
用户输入 → 关键词提取 → 知识库匹配（相似度 > 60%）
    ├── 命中 → 直接返回预设答案（< 50ms）
    └── 未命中 → 调用 DeepSeek API → 返回 AI 回答
```

相似度计算：基于关键词共现 + Jaccard 相似度。

### 3.3 不合规问题策略

| 类型 | 示例 | 小猫回复 |
|------|------|---------|
| 非系统相关 | "今天天气怎么样？" | "喵~ 我是进销存系统的小助手，只能回答系统操作相关的问题哦！试试问我"如何添加商品"吧~" |
| 敏感/违规 | 政治、色情等 | "喵？这个问题超出我的范围啦~ 请问关于系统操作的任何问题，我都会热情解答！" |
| 攻击性 | 辱骂等 | "喵... 我只想帮你用好进销存系统，有什么操作上的困难可以告诉我~" |
| 空消息 | 空输入 | "喵？你什么都没说呢~ 输入你的问题，我会尽力帮你！" |

前端本地关键词过滤 + 后端 System Prompt 兜底约束。

---

## 四、组件设计与交互

### 4.1 组件树

```
CatAgent.vue (总控)
├── CatAvatar.vue          ← 小猫形象层
│   ├── <video>            ← 播放 cat-agent.mp4（循环）
│   ├── 状态气泡            ← "需要帮助吗？" / "喵~"
│   └── 模式角标            ← 灵动 / 安静 / 折叠图标
├── ChatDialog.vue         ← 对话面板
│   ├── 消息列表            ← 用户消息 + 小猫回复
│   ├── 快捷问题标签        ← "如何添加商品？" 等高频问题
│   ├── 输入框 + 发送按钮
│   └── 设置入口            ← 打开模式切换
└── SettingsPanel.vue      ← 设置面板（小型弹窗）
    ├── 模式选择            ← 灵动(默认) / 安静 / 折叠
    └── 关于信息
```

### 4.2 三种模式

| 模式 | 小猫状态 | 对话面板 | 主动提示 |
|------|---------|---------|---------|
| **🐾 灵动模式** (默认) | 呼吸动画 + 偶尔跳跃，播放 MP4 | 点击打开 | ✅ 检测页面停留超 30s 弹出"需要帮助吗？" |
| **🌙 安静模式** | 仅呼吸动画，不跳跃 | 点击打开 | ❌ 不主动打扰 |
| **📌 折叠模式** | 缩小为小图标，收起 | 始终关闭 | ❌ 最小化存在 |

### 4.3 拖拽交互

- 小猫可拖拽到屏幕任意位置（鼠标/触屏）
- 拖拽边界限制：距屏幕边缘 ≥ 10px
- 位置记忆：存 `localStorage`，刷新后恢复
- 双击小猫：重置到默认位置（右下角，距右 20px，距底 20px）

### 4.4 对话面板

- 宽 360px，高 480px，从小猫位置弹出
- 消息气泡样式：用户右侧蓝紫色，小猫左侧暖白色
- 支持 Enter 发送
- 快捷问题标签（横向滚动），点击即问
- 每次对话最多显示 50 条消息，超出自动清除早期消息

### 4.5 小猫视觉呈现（方案二：视频 + CSS 组合）

- 小猫本体：`<video>` 循环播放 `cat-agent.mp4`
- 小猫尺寸：约 80×80px，折叠模式 40×40px
- 白底融合：视频白色背景通过 `mix-blend-mode` 或圆形容器 + 柔边遮罩与页面融合
- 状态动画（CSS overlay）：
  - 呼吸：`transform: scale(1.0 ~ 1.05)` 循环
  - 跳跃：`translateY(-10px)` 弹跳动画
  - 说话：嘴部微动叠加

---

## 五、后端 API 设计

### 5.1 接口

**POST `/api/agent/chat`**

```
Request:
{
  "message": "如何添加商品？",
  "history": [
    { "role": "user", "content": "你好" },
    { "role": "assistant", "content": "喵~ 你好！" }
  ]
}

Response:
{
  "code": 200,
  "data": { "reply": "喵~ 添加商品..." }
}
```

### 5.2 DeepSeek 配置

```yaml
# application.yml 新增
agent:
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:your-default-key}
    api-url: https://api.deepseek.com/v1/chat/completions
    model: deepseek-chat
```

### 5.3 System Prompt

> "你是超市进销存管理系统的AI助手，形象是一只白色小猫。你只回答与进销存系统操作相关的问题。回答风格：亲切、简洁、带'喵~'语气词。遇到无关问题，友好拒绝并引导用户回到系统操作话题。系统包含以下模块：员工管理、会员管理、商品管理、采购管理（含采购退货）、供应商管理。用户角色分为普通用户和管理员（一号/二号/三号管理员），不同角色有不同的操作权限。"

---

## 六、文件清单

### 前端新增

| 文件 | 作用 |
|------|------|
| `frontend/src/components/cat/CatAgent.vue` | 总控组件 |
| `frontend/src/components/cat/CatAvatar.vue` | 小猫形象（视频播放 + 拖拽 + 状态） |
| `frontend/src/components/cat/ChatDialog.vue` | 对话面板 |
| `frontend/src/components/cat/SettingsPanel.vue` | 模式设置面板 |
| `frontend/src/utils/knowledgeBase.js` | 本地知识库(40+ Q&A) |
| `frontend/src/api/agent.js` | 前端 API 封装 |

### 前端修改

| 文件 | 修改内容 |
|------|---------|
| `frontend/src/views/Layout.vue` | 引入 `<CatAgent />`，仅在登录后显示 |

### 后端新增

| 文件 | 作用 |
|------|------|
| `backend/src/main/java/com/supermarket/backend/controller/AgentController.java` | `/api/agent/*` 接口 |
| `backend/src/main/java/com/supermarket/backend/service/AgentService.java` | DeepSeek API 调用 |
| `backend/src/main/java/com/supermarket/backend/dto/ChatRequest.java` | 请求 DTO |
| `backend/src/main/java/com/supermarket/backend/dto/ChatResponse.java` | 响应 DTO |

### 后端修改

| 文件 | 修改内容 |
|------|---------|
| `backend/src/main/resources/application.yml` | 新增 `agent.deepseek.*` 配置 |

---

## 七、实现顺序

1. 后端：DTO → Service → Controller → 配置
2. 前端：knowledgeBase.js → agent.js → CatAvatar.vue → ChatDialog.vue → SettingsPanel.vue → CatAgent.vue
3. 集成：Layout.vue 引入 CatAgent
4. 联调测试
