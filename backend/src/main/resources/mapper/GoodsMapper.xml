<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.backend.mapper.GoodsMapper">

    <resultMap id="BaseResultMap" type="com.supermarket.backend.entity.Goods">
        <id column="id" property="id"/>
        <result column="goods_code" property="goodsCode"/>
        <result column="goods_name" property="goodsName"/>
        <result column="price" property="price"/>
        <result column="supplier_id" property="supplierId"/>
        <result column="intro" property="intro"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from goods
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into goods(goods_code, goods_name, price, supplier_id, intro, remark)
        values(#{goodsCode}, #{goodsName}, #{price}, #{supplierId}, #{intro}, #{remark})
    </insert>

    <update id="update">
        update goods
        set goods_code = #{goodsCode},
        goods_name = #{goodsName},
        price = #{price},
        supplier_id = #{supplierId},
        intro = #{intro},
        remark = #{remark}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from goods where id = #{id}
    </delete>

</mapper>