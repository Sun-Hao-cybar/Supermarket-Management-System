<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.backend.mapper.PurchaseMainMapper">
    <resultMap id="BaseResultMap" type="com.supermarket.backend.entity.PurchaseMain">
        <id column="id" property="id"/>
        <result column="purchase_no" property="purchaseNo"/>
        <result column="user_id" property="userId"/>
        <result column="total_num" property="totalNum"/>
        <result column="total_price" property="totalPrice"/>
        <result column="purchase_time" property="purchaseTime"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from purchase_main
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        select * from purchase_main where id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_main(purchase_no,user_id,total_num,total_price,purchase_time,remark)
        values(#{purchaseNo},#{userId},#{totalNum},#{totalPrice},#{purchaseTime},#{remark})
    </insert>

    <update id="update">
        update purchase_main
        set purchase_no=#{purchaseNo},user_id=#{userId},total_num=#{totalNum},
        total_price=#{totalPrice},purchase_time=#{purchaseTime},remark=#{remark}
        where id=#{id}
    </update>

    <delete id="deleteById">
        delete from purchase_main where id=#{id}
    </delete>
</mapper>