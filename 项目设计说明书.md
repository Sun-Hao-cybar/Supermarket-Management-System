# 超市进销存管理系统 — 项目设计说明书

> **版本**: v1.0  
> **日期**: 2026-06-05  
> **开发团队**: Sun Hao  
> **技术栈**: Spring Boot 4.0.6 + MyBatis 4.0.1 + MySQL 8.0 + Vue 3 + Element Plus  

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构设计](#2-系统架构设计)
3. [功能模块设计](#3-功能模块设计)
   - 3.1 [模块总览](#31-模块总览)
   - 3.2 [三级管理员权限矩阵](#32-三级管理员权限矩阵)
   - 3.3 [采购模块详细设计](#33-采购模块详细设计)
4. [后端详细设计](#4-后端详细设计)
   - 4.1 [实体类设计](#41-实体类设计)
   - 4.2 [统一响应格式](#42-统一响应格式)
   - 4.3 [MyBatis 配置](#43-mybatis-配置)
   - 4.4 [核心业务逻辑](#44-核心业务逻辑)
5. [前端详细设计](#5-前端详细设计)
   - 5.1 [路由设计](#51-路由设计)
   - 5.2 [组件树](#52-组件树)
   - 5.3 [状态管理](#53-状态管理)
   - 5.4 [组件间通信](#54-组件间通信)
   - 5.5 [采购管理页面设计](#55-采购管理页面设计)
6. [API 接口设计](#6-api-接口设计)
7. [权限管理设计](#7-权限管理设计)
8. [用户认证与注册流程](#8-用户认证与注册流程)
9. [Excel 导入导出设计](#9-excel-导入导出设计)
10. [国际化电话号码设计](#10-国际化电话号码设计)
11. [部署说明](#11-部署说明)
12. [开发规范](#12-开发规范)
13. [安全设计](#13-安全设计)
14. [未来改进方向](#14-未来改进方向)

---

## 1. 项目概述

### 1.1 项目背景

超市进销存管理系统是一个基于 B/S 架构的 Web 应用，旨在帮助超市管理人员高效地管理商品进货、销售、库存以及相关业务数据。系统支持管理员和普通用户两种角色，提供供应商管理、商品管理、员工管理、采购管理、会员管理五大核心业务模块。

### 1.2 项目目标

- 实现超市日常进销存业务的数字化管理
- 支持管理员通过 Excel 批量导入/导出数据，提升工作效率
- 实现多级管理员权限分权管理
- 支持国际化的电话号码格式验证
- 提供友好的用户界面和操作体验

### 1.3 功能范围

| 角色 | 功能模块 |
|------|----------|
| **管理员** | 供应商管理、商品管理、员工管理、采购管理、会员管理 |
| **普通用户** | 个人信息查看、商品信息查看、采购信息查看 |

### 1.4 技术选型

| 层次 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 4.0.6 |
| ORM 框架 | MyBatis (Spring Boot Starter) | 4.0.1 |
| 数据库 | MySQL | 8.0.44 |
| 编程语言 | Java | 21 |
| 构建工具 | Maven | 3.9+ |
| Excel 处理 | Apache POI | 5.2.5 |
| 前端框架 | Vue 3 (Composition API) | 3.5.34 |
| UI 组件库 | Element Plus | 2.14.1 |
| 构建工具 | Vite | 8.0.12 |
| HTTP 客户端 | Axios | — |
| 路由 | Vue Router | — |

---

## 2. 系统架构设计

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────┐
│                        浏览器 (Browser)                    │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │                  Vue 3 前端 (Frontend)              │  │
│  │  ┌────────┐ ┌──────────┐ ┌──────────────────────┐ │  │
│  │  │ Views  │ │  Router  │ │   API (Axios)        │ │  │
│  │  │ 组件    │ │  路由     │ │   接口调用            │ │  │
│  │  └────────┘ └──────────┘ └──────────┬───────────┘ │  │
│  └──────────────────────────────────────┼──────────────┘  │
└─────────────────────────────────────────┼─────────────────┘
                                          │ HTTP/JSON
                                          │ (CORS 配置)
                                          ▼
┌──────────────────────────────────────────────────────────┐
│                  Spring Boot 后端 (Backend)                │
│                                                          │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │Controller│→│   Service    │→│   Mapper (MyBatis)  │  │
│  │ 控制层   │  │   业务层      │  │    数据访问层        │  │
│  └──────────┘  └──────────────┘  └─────────┬─────────┘  │
│                                             │            │
│                       ┌─────────────────────┼───────┐    │
│                       │    ExcelUtil        │       │    │
│                       │  (Apache POI)       │       │    │
│                       └─────────────────────┘       │    │
└─────────────────────────────────────────────────────┼────┘
                                                      │ JDBC
                                                      ▼
┌──────────────────────────────────────────────────────────┐
│                 MySQL 8.0 (supermarket_db)                 │
│                                                          │
│  ┌────────┐ ┌──────────┐ ┌───────────┐ ┌─────────┐     │
│  │sys_user│ │ supplier │ │   goods   │ │ member  │     │
│  └────────┘ └──────────┘ └───────────┘ └─────────┘     │
│  ┌───────────────┐ ┌───────────────────┐                │
│  │ purchase_main │ │ purchase_detail   │                │
│  └───────────────┘ └───────────────────┘                │
└──────────────────────────────────────────────────────────┘
```

### 2.2 项目目录结构

```
Supermarket-Management-System/
├── README.md                    # 项目说明与部署文档
├── 数据库设计报告.md              # 数据库设计文档
├── 项目设计说明书.md              # 本文件
├── database/                    # 数据库脚本
│   └── ...-dump.sql             # MySQL 导出文件
├── backend/                     # 后端代码 (Spring Boot)
│   ├── pom.xml                  # Maven 配置
│   └── src/
│       ├── main/java/com/supermarket/backend/
│       │   ├── BackendApplication.java    # 启动类
│       │   ├── common/Result.java         # 统一响应封装
│       │   ├── config/CorsConfig.java     # CORS 配置
│       │   ├── controller/                # 控制器层 (6 个)
│       │   ├── entity/                    # 实体类 (6 个)
│       │   ├── mapper/                    # Mapper 接口 (6 个)
│       │   ├── service/                   # 业务服务层 (6 个)
│       │   └── util/ExcelUtil.java        # Excel 工具类
│       └── resources/
│           ├── application.yml            # 应用配置
│           └── mapper/                    # MyBatis XML (6 个)
└── frontend/                     # 前端代码 (Vue 3)
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── main.js               # 入口文件
        ├── App.vue               # 根组件
        ├── style.css             # 全局样式
        ├── router/index.js       # 路由配置
        ├── utils/request.js      # Axios 封装
        ├── api/                  # API 接口模块 (6 个)
        └── views/                # 页面组件 (8 个)
```

### 2.3 分层架构说明

后端采用经典的三层架构：

| 层次 | 包名 | 职责 | 示例 |
|------|------|------|------|
| **Controller 层** | `controller/` | 接收 HTTP 请求，参数校验，调用 Service，返回统一格式 Result | `GoodsController.java` |
| **Service 层** | `service/` | 核心业务逻辑处理，事务管理，数据校验 | `SysUserService.java` |
| **Mapper 层** | `mapper/` | 数据访问接口，MyBatis XML 映射 | `GoodsMapper.java` + `GoodsMapper.xml` |
| **Entity 层** | `entity/` | 数据库表对应的 POJO 实体 | `Goods.java` |
| **Common 层** | `common/` | 通用响应封装 `Result<T>` | `Result.java` |
| **Config 层** | `config/` | 配置类（CORS） | `CorsConfig.java` |
| **Util 层** | `util/` | 工具类（Excel 读写） | `ExcelUtil.java` |

---

## 3. 功能模块设计

### 3.1 模块总览

```
超市进销存管理系统
│
├── 登录注册模块
│   ├── 管理员登录（账号 6 位，11/10/01 开头）
│   ├── 普通用户登录（账号 9 位，00 开头）
│   ├── 用户注册（含密码强度检测、国际电话验证）
│   └── 角色选择（管理员/普通用户）
│
├── 管理员功能模块
│   ├── A. 供应商管理（一号、二号管理员）
│   │   ├── 供应商列表查询
│   │   ├── 供应商新增/修改/删除
│   │   ├── Excel 批量导入
│   │   └── Excel 导出
│   │
│   ├── B. 商品管理（一号、二号管理员）
│   │   ├── 商品列表查询（关联供应商名称）
│   │   ├── 商品新增/修改/删除
│   │   ├── Excel 批量导入
│   │   └── Excel 导出
│   │
│   ├── C. 员工管理（一号、三号管理员）
│   │   ├── 员工列表查询（含管理员身份显示）
│   │   ├── 员工新增/修改/删除
│   │   ├── Excel 批量导入
│   │   ├── Excel 导出
│   │   └── 管理员数量限制（最多 3 人）
│   │
│   ├── D. 采购管理（一号、三号管理员）
│   │   ├── 采购主表 CRUD
│   │   ├── 采购明细 CRUD
│   │   ├── Excel 批量导入（主表/明细分别）
│   │   └── Excel 导出（主表/明细分别）
│   │
│   └── E. 会员管理（所有管理员）
│       ├── 会员列表查询
│       ├── 会员新增/修改/删除
│       └── Excel 导出
│
└── 普通用户功能模块
    ├── 个人信息查看
    ├── 商品信息查看（支持导出）
    └── 采购信息查看（主表+明细，支持导出）
```

### 3.2 三级管理员权限矩阵

| 功能 | 一号管理员 (11xxx) | 二号管理员 (10xxx) | 三号管理员 (01xxx) | 普通用户 |
|------|:---:|:---:|:---:|:---:|
| **供应商管理** | ✅ 增删改查 | ✅ 增删改查 | 👁️ 仅查看 | ❌ |
| **商品管理** | ✅ 增删改查 | ✅ 增删改查 | 👁️ 仅查看 | 👁️ 仅查看 |
| **员工管理** | ✅ 增删改查 | 👁️ 仅查看 | ✅ 增删改查 | ❌ |
| **采购管理** | ✅ 增删改查 | 👁️ 仅查看 | ✅ 增删改查 | 👁️ 仅查看 |
| **会员管理** | ✅ 增删改查 | ✅ 增删改查 | ✅ 增删改查 | ❌ |
| **个人信息** | ✅ | ✅ | ✅ | ✅ |
| **Excel 导入** | ✅ | ✅ (部分) | ✅ (部分) | ❌ |
| **Excel 导出** | ✅ | ✅ | ✅ | ✅ (部分) |

---

### 3.3 采购模块详细设计

采购模块是本系统核心业务模块之一，采用**主表-明细**（Master-Detail）模式设计，实现采购订单的完整管理。

#### 3.3.1 模块架构

```
┌─────────────────────────────────────────────────────────────┐
│                    采购管理模块                                │
│                                                             │
│  ┌─────────────────────┐    ┌─────────────────────────────┐ │
│  │   PurchaseMain      │    │    PurchaseDetail           │ │
│  │   (采购主表)          │◄───│    (采购明细)                │ │
│  │                     │    │                             │ │
│  │ ┌─────────────────┐ │    │ ┌─────────────────────────┐ │ │
│  │ │ purchaseNo (PK) │ │    │ │ detailNo (PK)           │ │ │
│  │ │ userId (FK)     │ │    │ │ purchaseNo (逻辑关联)     │ │ │
│  │ │ totalNum        │ │    │ │ goodsId (FK → goods)    │ │ │
│  │ │ totalPrice      │ │    │ │ goodsNum                │ │ │
│  │ │ purchaseTime    │ │    │ │ goodsPrice              │ │ │
│  │ │ remark          │ │    │ │ totalPrice              │ │ │
│  │ │ createTime      │ │    │ │ remark                  │ │ │
│  │ └─────────────────┘ │    │ └─────────────────────────┘ │ │
│  └────────┬────────────┘    └──────────────┬──────────────┘ │
│           │ FK                             │ FK              │
│           ▼                                ▼                │
│  ┌────────────────┐              ┌──────────────────┐       │
│  │   sys_user     │              │     goods        │       │
│  │   (员工表)      │              │   (商品表)        │       │
│  │                │              │                  │       │
│  │  id ───────────┼──────┐       │  id ─────────────┼──┐    │
│  │  username      │      │       │  goodsCode       │  │    │
│  │  realName      │      │       │  goodsName       │  │    │
│  └────────────────┘      │       │  price           │  │    │
│                          │       │  supplierId (FK) │  │    │
│                          │       └──────────────────┘  │    │
│                          │              │               │    │
│                          │              ▼               │    │
│                          │    ┌──────────────────┐      │    │
│                          │    │    supplier      │      │    │
│                          │    │  (供应商表)       │      │    │
│                          │    │  supplierName    │      │    │
│                          │    └──────────────────┘      │    │
│                          │                              │    │
│  ┌───────────────────────┼──────────────────────────────┘    │
│  │        数据流向        │                                    │
│  │                       │                                    │
│  │  purchase_main ──FK──→ sys_user (采购经办人)               │
│  │  purchase_main ────→ purchase_detail (通过 purchaseNo)     │
│  │  purchase_detail ──FK──→ goods (采购商品)                  │
│  │  goods ──FK──→ supplier (商品供应商)                       │
│  └───────────────────────────────────────────────────────────┘
```

#### 3.3.2 主表-明细关联机制

采购主表与明细之间通过 **`purchase_no`（采购清单号）** 进行逻辑关联，而非数据库外键：

| 关联方式 | 字段 | 说明 |
|----------|------|------|
| **逻辑关联** | `purchase_main.purchase_no` ↔ `purchase_detail.purchase_no` | 业务层面的关联，非数据库 FK |
| **物理外键** | `purchase_main.user_id` → `sys_user.id` | ON DELETE CASCADE |
| **物理外键** | `purchase_detail.goods_id` → `goods.id` | ON DELETE CASCADE |

> **设计说明**: `purchase_no` 采用逻辑关联而非物理外键的原因：
> 1. 采购单号是业务标识，可能涉及跨系统数据交换
> 2. 明细可以独立于主表进行部分操作
> 3. 避免级联删除导致明细数据意外丢失

#### 3.3.3 采购业务流程

```
┌────────────────────────────────────────────────────────────────┐
│                    采购管理完整业务流程                           │
│                                                                │
│  1. 创建采购主表                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 选择经办员工 → 输入采购清单号 → 填写采购数量 → 填写采购总价   │  │
│  │ → 选择采购日期 → 添加备注 → 提交                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            │                                    │
│                            ▼                                    │
│  2. 创建采购明细（关联到主表）                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 输入明细编号 → 输入关联的采购清单号 → 选择商品 → 填写数量    │  │
│  │ → 填写单价 → 计算总价 → 添加备注 → 提交                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            │                                    │
│                            ▼                                    │
│  3. 数据查询与核对                                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 主表列表展示所有采购单 → 明细列表展示所有明细行              │  │
│  │ → 通过 purchaseNo 核对主表与明细的一致性                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            │                                    │
│                            ▼                                    │
│  4. 数据维护（修改/删除）                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 编辑主表 → 编辑明细 → 删除主表 → 删除明细                   │  │
│  │ 注意：删除主表时不自动删除关联明细（非FK级联）               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            │                                    │
│                            ▼                                    │
│  5. Excel 批量操作                                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 导入主表 Excel → 导入明细 Excel → 导出主表 Excel            │  │
│  │ → 导出明细 Excel                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

#### 3.3.4 前端双表设计

采购管理页面 ([Purchase.vue](frontend/src/views/Purchase.vue)) 采用**上下双表布局**，在一个页面内同时展示主表和明细：

```
┌────────────────────────────────────────────────────┐
│  采购管理                                           │
│                                                    │
│  [新增采购单] [导入主表Excel] [导出主表Excel]         │
│  ┌──────────────────────────────────────────────┐  │
│  │ 采购主表                                      │  │
│  │ ┌─────────┬────────┬──────┬──────┬────┬────┐ │  │
│  │ │清单号    │员工编号 │数量   │总价   │时间│备注│ │  │
│  │ ├─────────┼────────┼──────┼──────┼────┼────┤ │  │
│  │ │PO...    │110001  │100   │350.00│... │... │ │  │
│  │ └─────────┴────────┴──────┴──────┴────┴────┘ │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  采购明细                                           │
│  [新增明细] [导入明细Excel] [导出明细Excel]          │
│  ┌──────────────────────────────────────────────┐  │
│  │ 采购明细                                      │  │
│  │ ┌──────┬─────────┬──────┬─────┬─────┬────┬──┐ │  │
│  │ │明细号 │清单号    │商品ID│数量  │单价  │总价│..│ │  │
│  │ ├──────┼─────────┼──────┼─────┼─────┼────┼──┤ │  │
│  │ │PD... │PO...    │1     │100  │3.50 │350 │..│ │  │
│  │ └──────┴─────────┴──────┴─────┴─────┴────┴──┘ │  │
│  └──────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

**设计要点**：
- 主表编辑弹窗中，员工字段使用 `<el-select>` 下拉选择（从用户列表加载）
- 明细编辑弹窗中，商品编号字段使用纯文本输入（当前版本未实现下拉选择）
- 两表各自独立加载数据和操作，互不阻塞
- 权限控制：`hasManagePermission = role === '1' && (adminLevel === 1 || adminLevel === 3)`

#### 3.3.5 数据字段详解

**PurchaseMain（采购主表）**：

| 字段 | Java 类型 | 数据库类型 | 说明 | 示例值 |
|------|-----------|------------|------|--------|
| `id` | `Long` | `BIGINT PK` | 自增主键 | `1` |
| `purchaseNo` | `String` | `VARCHAR(50) UNIQUE` | 采购清单号，业务唯一标识 | `PO20240101001` |
| `userId` | `Long` | `BIGINT FK` | 经办员工ID → sys_user.id | `5` |
| `totalNum` | `Integer` | `INT` | 采购总数量 | `100` |
| `totalPrice` | `BigDecimal` | `DECIMAL(12,2)` | 采购总金额 | `350.00` |
| `purchaseTime` | `Date` | `DATETIME` | 采购日期 | `2024-01-01 08:00` |
| `remark` | `String` | `VARCHAR(500)` | 备注 | — |
| `createTime` | `Date` | `DATETIME` | 记录创建时间（自动填充） | `2026-06-04 11:11:59` |

**PurchaseDetail（采购明细）**：

| 字段 | Java 类型 | 数据库类型 | 说明 | 示例值 |
|------|-----------|------------|------|--------|
| `id` | `Long` | `BIGINT PK` | 自增主键 | `1` |
| `detailNo` | `String` | `VARCHAR(50) UNIQUE` | 明细编号，唯一标识 | `PD20240101001` |
| `purchaseNo` | `String` | `VARCHAR(50)` | 关联的采购清单号（逻辑关联） | `PO20240101001` |
| `goodsId` | `Long` | `BIGINT FK` | 商品ID → goods.id | `1` |
| `goodsNum` | `Integer` | `INT` | 该明细的采购数量 | `100` |
| `goodsPrice` | `BigDecimal` | `DECIMAL(10,2)` | 商品采购单价 | `3.50` |
| `totalPrice` | `BigDecimal` | `DECIMAL(12,2)` | 明细总价 = goodsNum × goodsPrice | `350.00` |
| `remark` | `String` | `VARCHAR(500)` | 备注 | — |

#### 3.3.6 API 接口详解

采购模块共有 12 个 API 接口（主表 6 个 + 明细 6 个）：

| # | 方法 | 路径 | 说明 | 权限 |
|---|------|------|------|------|
| 1 | GET | `/purchaseMain/list` | 查询所有采购主表记录 | 登录用户 |
| 2 | POST | `/purchaseMain/add` | 新增采购单 | 一号/三号管理员 |
| 3 | POST | `/purchaseMain/update` | 修改采购单 | 一号/三号管理员 |
| 4 | GET | `/purchaseMain/delete?id=` | 删除采购单 | 一号/三号管理员 |
| 5 | POST | `/purchaseMain/import` | Excel 批量导入主表 | 一号/三号管理员 |
| 6 | GET | `/purchaseMain/export` | Excel 导出主表 | 登录用户 |
| 7 | GET | `/purchaseDetail/list` | 查询所有采购明细 | 登录用户 |
| 8 | POST | `/purchaseDetail/add` | 新增采购明细 | 一号/三号管理员 |
| 9 | POST | `/purchaseDetail/update` | 修改采购明细 | 一号/三号管理员 |
| 10 | GET | `/purchaseDetail/delete?id=` | 删除采购明细 | 一号/三号管理员 |
| 11 | POST | `/purchaseDetail/import` | Excel 批量导入明细 | 一号/三号管理员 |
| 12 | GET | `/purchaseDetail/export` | Excel 导出明细 | 登录用户 |

**请求/响应示例**：

```json
// POST /purchaseMain/add
{
  "purchaseNo": "PO20240101001",
  "userId": 5,
  "totalNum": 100,
  "totalPrice": 350.00,
  "purchaseTime": "2024-01-01 08:00",
  "remark": "紧急采购"
}
// Response: { "code": 200, "msg": "采购单新增成功", "data": null }

// POST /purchaseDetail/add
{
  "detailNo": "PD20240101001",
  "purchaseNo": "PO20240101001",
  "goodsId": 1,
  "goodsNum": 100,
  "goodsPrice": 3.50,
  "totalPrice": 350.00,
  "remark": null
}
// Response: { "code": 200, "msg": "采购明细新增成功", "data": null }
```

#### 3.3.7 Excel 导入导出规范

| 操作 | 文件名 | 表头字段 |
|------|--------|----------|
| 主表导入 | — | `purchaseNo`, `userId`, `totalNum`, `totalPrice`, `purchaseTime`, `remark` |
| 主表导出 | `采购主表数据.xlsx` | 采购清单号, 员工编号, 采购数量, 采购总价, 采购时间, 备注 |
| 明细导入 | — | `detailNo`, `purchaseNo`, `goodsId`, `goodsNum`, `goodsPrice`, `totalPrice`, `remark` |
| 明细导出 | `采购明细数据.xlsx` | 明细号, 采购清单号, 商品编号, 采购数量, 商品单价, 商品总价, 备注 |

> **注意**: 主表导入时，`purchaseTime` 字段使用 `DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")` 解析，解析失败时自动使用当前时间。

#### 3.3.8 后端代码结构

```
Controller 层
├── PurchaseMainController.java     # 主表 REST 接口
│   └── /purchaseMain/{list, add, update, delete, import, export}
└── PurchaseDetailController.java   # 明细 REST 接口
    └── /purchaseDetail/{list, add, update, delete, import, export}

Service 层
├── PurchaseMainService.java        # 主表业务逻辑（当前为简单透传）
└── PurchaseDetailService.java      # 明细业务逻辑（当前为简单透传）

Mapper 层
├── PurchaseMainMapper.java         # 主表数据访问接口
├── PurchaseMainMapper.xml          # 主表 SQL 映射（selectAll/insert/update/deleteById）
├── PurchaseDetailMapper.java       # 明细数据访问接口
└── PurchaseDetailMapper.xml        # 明细 SQL 映射（selectAll/insert/update/deleteById）

Entity 层
├── PurchaseMain.java               # 主表实体（8 个字段，含 @JsonFormat 日期格式化）
└── PurchaseDetail.java             # 明细实体（8 个字段）
```

#### 3.3.9 业务规则与约束

| 规则类型 | 约束内容 | 实现位置 |
|----------|----------|----------|
| **唯一性** | `purchase_no` 唯一，不可重复 | 数据库 UNIQUE KEY |
| **唯一性** | `detail_no` 唯一，不可重复 | 数据库 UNIQUE KEY |
| **级联删除** | 删除 `sys_user` 时，其关联的采购主表记录同步删除 | FK ON DELETE CASCADE |
| **级联删除** | 删除 `goods` 时，其关联的采购明细记录同步删除 | FK ON DELETE CASCADE |
| **逻辑关联** | 主表与明细通过 `purchase_no` 关联，不是物理外键 | 业务层约定 |
| **权限控制** | 仅一号管理员（11xxx）和三号管理员（01xxx）可增删改 | 前端 `hasManagePermission` + 后端预留 |
| **编号规范** | 采购清单号建议格式：`PO` + 日期 + 序号（如 `PO20240101001`） | 手动输入，无自动生成 |
| **编号规范** | 明细编号建议格式：`PD` + 日期 + 序号（如 `PD20240101001`） | 手动输入，无自动生成 |

#### 3.3.10 已知问题与改进建议

| 问题 | 严重程度 | 说明 | 建议方案 |
|------|:---:|------|----------|
| **明细商品选择体验差** | 🟡 中 | 明细编辑时 `goodsId` 需手动输入数字，无法选择商品 | 改为 `<el-select>` 下拉选择商品 |
| **无自动编号** | 🟡 中 | 采购清单号和明细编号均需手动输入，容易重复 | 后端自动生成 PO/PD 编号 |
| **无明细级联加载** | 🟡 中 | 选择主表记录后，明细不会自动筛选关联记录 | 添加主表明细联动：点击主表行 → 过滤关联明细 |
| **无金额自动计算** | 🟢 低 | `totalPrice` 需手动输入，不会自动按数量×单价计算 | 前端添加计算逻辑 `totalPrice = goodsNum × goodsPrice` |
| **无主表汇总校验** | 🟢 低 | 主表的 `totalNum`/`totalPrice` 与明细汇总不对账 | 添加汇总校验：主表汇总 = SUM(明细) |
| **删除主表不级联明细** | 🟢 低 | 删除主表时关联明细残留 | 添加删除主表时同步删除关联明细的逻辑 |

---

## 4. 后端详细设计

### 4.1 实体类设计

系统共有 6 个实体类，均使用 Lombok `@Data` 注解简化 getter/setter。

#### 4.1.1 SysUser（系统用户）

```java
// 位置: backend/.../entity/SysUser.java
// 对应表: sys_user
@Data
public class SysUser {
    private Long id;
    private String username;    // 登录账号
    private String password;    // 登录密码
    private String realName;    // 真实姓名
    private String phone;       // 电话（格式: 区号|号码）
    private BigDecimal salary;  // 工资
    private Integer role;       // 1=管理员, 0=普通用户
    private String remark;      // 备注
    private Date createTime;    // 创建时间
}
```

#### 4.1.2 Supplier（供应商）

```java
// 位置: backend/.../entity/Supplier.java
// 对应表: supplier
@Data
public class Supplier {
    private Long id;
    private String supplierCode;   // 供应商编码
    private String supplierName;   // 供应商名称
    private String shortName;      // 简称
    private String address;        // 地址
    private String phone;          // 公司电话
    private String email;          // 邮箱
    private String contactPerson;  // 联系人
    private String contactPhone;   // 联系电话（国际格式）
    private String remark;         // 备注
    private Date createTime;       // 创建时间
}
```

#### 4.1.3 Goods（商品）

```java
// 位置: backend/.../entity/Goods.java
// 对应表: goods（LEFT JOIN supplier 获取 supplierName）
@Data
public class Goods {
    private Long id;
    private String goodsCode;       // 商品编号
    private String goodsName;       // 商品名称
    private BigDecimal price;       // 单价
    private Long supplierId;        // 供应商ID (FK)
    private String supplierName;    // 供应商名称（关联查询填充）
    private String intro;           // 商品简介
    private String remark;          // 备注
    private Date createTime;        // 创建时间
}
```

#### 4.1.4 Member（会员）

```java
// 位置: backend/.../entity/Member.java
// 对应表: member
@Data
public class Member {
    private Long id;
    private String memberNo;       // 会员编号
    private String name;           // 会员姓名
    private String phone;          // 电话（国际格式）
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm", timezone = "Asia/Shanghai")
    private Date registerTime;     // 注册时间
    private String remark;         // 备注
}
```

#### 4.1.5 PurchaseMain（采购主表）

```java
// 位置: backend/.../entity/PurchaseMain.java
// 对应表: purchase_main
// 关联: FK user_id → sys_user.id (ON DELETE CASCADE)
//      逻辑关联 purchase_no → purchase_detail.purchase_no
@Data
public class PurchaseMain {
    private Long id;
    private String purchaseNo;     // 采购清单号（UNIQUE，格式 PO + 日期 + 序号）
    private Long userId;           // 经办员工ID (FK → sys_user.id)
    private Integer totalNum;      // 采购总数量
    private BigDecimal totalPrice; // 采购总金额 DECIMAL(12,2)
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm", timezone = "Asia/Shanghai")
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm")
    private Date purchaseTime;     // 采购时间
    private String remark;         // 备注
    private Date createTime;       // 创建时间（自动填充 CURRENT_TIMESTAMP）
}
```

> **设计说明**: PurchaseMain 使用 `@JsonFormat` 和 `@DateTimeFormat` 双重注解确保日期格式在 JSON 序列化和请求参数绑定时均为 `yyyy-MM-dd HH:mm` 格式。

#### 4.1.6 PurchaseDetail（采购明细）

```java
// 位置: backend/.../entity/PurchaseDetail.java
// 对应表: purchase_detail
// 关联: FK goods_id → goods.id (ON DELETE CASCADE)
//      逻辑关联 purchase_no → purchase_main.purchase_no (非 FK)
@Data
public class PurchaseDetail {
    private Long id;
    private String detailNo;       // 明细编号（UNIQUE，格式 PD + 日期 + 序号）
    private String purchaseNo;     // 关联采购清单号（逻辑关联，非物理外键）
    private Long goodsId;          // 商品ID (FK → goods.id)
    private Integer goodsNum;      // 采购数量
    private BigDecimal goodsPrice; // 商品采购单价 DECIMAL(10,2)
    private BigDecimal totalPrice; // 明细总价 = goodsNum × goodsPrice DECIMAL(12,2)
    private String remark;         // 备注
}
```

> **设计说明**: PurchaseDetail 没有 `createTime` 字段（数据库表无此列），且 `purchaseNo` 采用逻辑关联而非物理外键，允许主表和明细独立维护。`totalPrice` 理论上应为 `goodsNum × goodsPrice`，但当前版本未做自动计算校验。

### 4.2 统一响应格式

所有 API 统一使用 `Result<T>` 封装返回：

```java
// 位置: backend/.../common/Result.java
@Data
public class Result<T> {
    private Integer code;  // 200=成功, 500=失败
    private String msg;    // 提示信息
    private T data;        // 返回数据

    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> error(String msg) { ... }
}
```

返回 JSON 示例：
```json
// 成功
{ "code": 200, "msg": "操作成功", "data": [...] }

// 失败
{ "code": 500, "msg": "用户名或密码错误", "data": null }
```

### 4.3 MyBatis 配置

```yaml
# application.yml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/supermarket_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Sh241612

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.supermarket.backend.entity
  configuration:
    map-underscore-to-camel-case: true  # 自动映射 goods_code → goodsCode
```

### 4.4 核心业务逻辑

#### 4.4.1 用户注册流程（SysUserService.add）

```
输入: SysUser (username, password, phone, role, ...)
  │
  ├─ 1. 解析电话号码: 分离区号和号码
  │
  ├─ 2. 角色判断
  │   ├─ role == 1 (管理员):
  │   │   ├─ 检查用户名长度是否为 6 位
  │   │   ├─ 检查前缀是否为 11/10/01
  │   │   ├─ 检查管理员总数是否 < 3
  │   │   ├─ 检查该前缀的管理员是否已存在
  │   │   └─ 截断用户名为 6 位
  │   │
  │   └─ role == 0 (普通用户):
  │       ├─ 检查用户名以 00 开头
  │       ├─ 检查员工表中是否存在该用户（管理员已预录入）
  │       ├─ 检查该员工是否已设置密码（即已注册）
  │       └─ 截断用户名为 9 位
  │
  ├─ 3. 密码验证
  │   ├─ 长度 >= 8
  │   ├─ 包含大小写字母 + 数字 + 特殊字符(@$!%*?&)
  │   └─ 全局唯一性检查
  │
  ├─ 4. 电话验证
  │   ├─ 根据区号验证号码位数和格式
  │   └─ 同区号下唯一性检查
  │
  ├─ 5. 用户名唯一性检查
  │
  └─ 6. 插入数据库
```

#### 4.4.2 权限判断逻辑（SysUserService.getAdminLevel）

```java
public int getAdminLevel(String username) {
    if (username.startsWith("11")) return 1;  // 一号管理员（最高权限）
    if (username.startsWith("10")) return 2;  // 二号管理员
    if (username.startsWith("01")) return 3;  // 三号管理员
    return 0;                                  // 普通用户
}

public boolean canManageSupplier(int level) { return level == 1 || level == 2; }
public boolean canManageGoods(int level) { return level == 1 || level == 2; }
public boolean canManageEmployee(int level) { return level == 1 || level == 3; }
public boolean canManagePurchase(int level) { return level == 1 || level == 3; }
```

#### 4.4.3 采购业务逻辑

**主表管理流程（PurchaseMainService）**：

```
请求入口（Controller）
  │
  ├─ GET /purchaseMain/list
  │   └─ PurchaseMainService.list()
  │       └─ PurchaseMainMapper.selectAll()
  │           └─ SQL: SELECT * FROM purchase_main
  │
  ├─ POST /purchaseMain/add
  │   └─ PurchaseMainService.add(PurchaseMain)
  │       └─ PurchaseMainMapper.insert(main)
  │           └─ SQL: INSERT INTO purchase_main(purchase_no, user_id, total_num, total_price, purchase_time, remark)
  │
  ├─ POST /purchaseMain/update
  │   └─ PurchaseMainService.update(PurchaseMain)
  │       └─ PurchaseMainMapper.update(main)
  │           └─ SQL: UPDATE purchase_main SET ... WHERE id = #{id}
  │
  └─ GET /purchaseMain/delete?id=
      └─ PurchaseMainService.delete(Long id)
          └─ PurchaseMainMapper.deleteById(id)
              └─ SQL: DELETE FROM purchase_main WHERE id = #{id}
```

**明细管理流程（PurchaseDetailService）**：

```
请求入口（Controller）
  │
  ├─ GET /purchaseDetail/list
  │   └─ PurchaseDetailService.list()
  │       └─ PurchaseDetailMapper.selectAll()
  │           └─ SQL: SELECT * FROM purchase_detail
  │
  ├─ POST /purchaseDetail/add
  │   └─ PurchaseDetailService.add(PurchaseDetail)
  │       └─ PurchaseDetailMapper.insert(detail)
  │           └─ SQL: INSERT INTO purchase_detail(detail_no, purchase_no, goods_id, goods_num, goods_price, total_price, remark)
  │
  ├─ POST /purchaseDetail/update
  │   └─ PurchaseDetailService.update(PurchaseDetail)
  │       └─ PurchaseDetailMapper.update(detail)
  │           └─ SQL: UPDATE purchase_detail SET ... WHERE id = #{id}
  │
  └─ GET /purchaseDetail/delete?id=
      └─ PurchaseDetailService.delete(Long id)
          └─ PurchaseDetailMapper.deleteById(id)
              └─ SQL: DELETE FROM purchase_detail WHERE id = #{id}
```

> **注意**: 当前版本 Service 层为简单透传，未实现以下高级逻辑：
> - 主表明细数量/金额自动汇总
> - 删除主表时级联删除关联明细
> - 采购单号自动生成
> - 明细总价自动计算（`goodsPrice × goodsNum`）
> 
> 这些可作为后续版本优化项。

---

## 5. 前端详细设计

### 5.1 路由设计

```javascript
// 位置: frontend/src/router/index.js
const routes = [
  {
    path: '/',
    name: 'Login',
    component: () => import('@/views/Login.vue')   // 登录注册页
  },
  {
    path: '/layout',
    component: () => import('@/views/Layout.vue'),  // 主布局（含侧边栏）
    children: [
      { path: 'user-info', component: () => import('@/views/UserInfo.vue') },
      { path: 'goods',     component: () => import('@/views/Goods.vue') },
      { path: 'purchase',  component: () => import('@/views/Purchase.vue') },
      { path: 'supplier',  component: () => import('@/views/Supplier.vue') },
      { path: 'employee',  component: () => import('@/views/Employee.vue') },
      { path: 'member',    component: () => import('@/views/Member.vue') },
    ]
  }
]
```

### 5.2 组件树

```
App.vue
└── <router-view />
    ├── Login.vue                          # 路径: /
    │   ├── 视频区域 (<video>)
    │   ├── 角色选择 (<el-radio-group>)
    │   ├── 登录选项卡
    │   │   ├── 账号输入
    │   │   ├── 密码输入
    │   │   └── 登录按钮
    │   └── 注册选项卡
    │       ├── 账号输入
    │       ├── 密码输入 + 密码强度指示器
    │       ├── 国际区号选择 + 手机号输入
    │       ├── 姓名输入
    │       └── 注册按钮
    │
    └── Layout.vue                         # 路径: /layout
        ├── 侧边栏 (<el-aside>)
        │   ├── 系统标题
        │   ├── 管理员身份显示
        │   └── 导航菜单 (<el-menu>)
        │       ├── 个人信息
        │       ├── 商品管理/信息
        │       ├── 采购管理/信息
        │       ├── 供应商管理
        │       ├── 员工管理
        │       ├── 会员管理
        │       └── 退出登录
        └── 主内容区 (<el-main>)
            └── <router-view />
                ├── UserInfo.vue          # 个人信息
                ├── Goods.vue             # 商品管理
                ├── Purchase.vue          # 采购管理
                ├── Supplier.vue          # 供应商管理
                ├── Employee.vue          # 员工管理
                └── Member.vue            # 会员管理
```

### 5.3 状态管理

系统采用 `localStorage` 进行客户端状态保持：

| 键名 | 类型 | 说明 |
|------|------|------|
| `role` | string | 用户角色: `'1'`=管理员, `'2'`=普通用户 |
| `userId` | string | 当前登录用户 ID |
| `username` | string | 当前登录用户名 |
| `adminLevel` | string | 管理员级别: `'1'`,`'2'`,`'3'`,`'0'` |

### 5.4 组件间通信

- **路由传参**: 不需要参数传递，各页面独立加载数据
- **localStorage**: 用于跨组件共享用户信息和权限
- **API 调用**: 每个视图组件独立调用 API 获取数据
- **事件总线**: 不使用，所有数据通过 API 重新获取

### 5.5 采购管理页面设计

[Purchase.vue](frontend/src/views/Purchase.vue) 是系统中最复杂的页面组件，采用**上下双表独立管理**的模式：

#### 5.5.1 页面结构

```
Purchase.vue
├── 采购主表区域
│   ├── 工具栏: [新增采购单] [导入主表Excel] [导出主表Excel]
│   ├── <el-table> :mainList
│   │   ├── 采购清单号 (purchaseNo)
│   │   ├── 员工编号 (userId)
│   │   ├── 采购数量 (totalNum)
│   │   ├── 采购总价 (totalPrice)
│   │   ├── 采购时间 (purchaseTime)
│   │   ├── 备注 (remark)
│   │   └── 操作列: [编辑] [删除]
│   └── 主表弹窗 <el-dialog>
│       ├── 采购清单号 <el-input>
│       ├── 员工 <el-select> ← 从 getUserList() 加载选项
│       ├── 采购数量 <el-input>
│       ├── 采购总价 <el-input>
│       ├── 采购时间 <el-date-picker> (YYYY-MM-DD)
│       └── 备注 <el-input>
│
├── 采购明细区域
│   ├── 工具栏: [新增明细] [导入明细Excel] [导出明细Excel]
│   ├── <el-table> :detailList
│   │   ├── 明细号 (detailNo)
│   │   ├── 采购清单号 (purchaseNo)
│   │   ├── 商品编号 (goodsId)
│   │   ├── 采购数量 (goodsNum)
│   │   ├── 商品单价 (goodsPrice)
│   │   ├── 商品总价 (totalPrice)
│   │   ├── 备注 (remark)
│   │   └── 操作列: [编辑] [删除]
│   └── 明细弹窗 <el-dialog>
│       ├── 明细号 <el-input>
│       ├── 采购清单号 <el-input>
│       ├── 商品编号 <el-input> (当前为纯文本，建议改为下拉选择)
│       ├── 采购数量 <el-input>
│       ├── 商品单价 <el-input>
│       ├── 商品总价 <el-input>
│       └── 备注 <el-input>
│
└── 隐藏的 <input type="file"> (主表导入 + 明细导入)
```

#### 5.5.2 数据加载

| 数据源 | API 调用 | 用途 |
|--------|----------|------|
| `mainList` | `getPurchaseMainList()` | 采购主表列表 |
| `detailList` | `getPurchaseDetailList()` | 采购明细列表 |
| `userList` | `getUserList()` | 主表弹窗中的员工下拉选择 |

三个数据源在 `onMounted` 中并行加载，互不依赖。

#### 5.5.3 权限控制

```javascript
// 仅一号和三号管理员有管理权限
const hasManagePermission = computed(() => {
  return role.value === '1' && (adminLevel.value === 1 || adminLevel.value === 3)
})
```

- `hasManagePermission = true`: 显示新增/编辑/删除/导入按钮
- `hasManagePermission = false`: 仅显示数据表格和导出按钮（只读模式）

#### 5.5.4 已知前端问题

| 问题 | 说明 |
|------|------|
| **明细商品选择为文本输入** | `goodsId` 字段使用 `<el-input>` 手动输入，需改为 `<el-select>` 从商品列表选择 |
| **无联动筛选** | 点击主表某条记录，明细表不会自动过滤出关联的明细行 |
| **无金额自动计算** | 明细的 `totalPrice` 需手动输入，不会按 `goodsNum × goodsPrice` 自动计算 |
| **员工编号直接显示** | 主表显示 `userId` 而非员工姓名，可读性差 |

---

## 6. API 接口设计

### 6.1 接口总览

基础 URL: `http://localhost:8080`

| 模块 | 前缀 | 接口数量 | 功能 |
|------|------|:---:|------|
| 用户管理 | `/user` | 11 | 登录、注册、增删改查、导入导出、检查员工 |
| 供应商管理 | `/supplier` | 6 | 增删改查、Excel 导入导出 |
| 商品管理 | `/goods` | 6 | 增删改查（含供应商关联）、Excel 导入导出 |
| 会员管理 | `/member` | 6 | 增删改查、导出 |
| 采购主表 | `/purchaseMain` | 6 | 增删改查、Excel 导入导出 |
| 采购明细 | `/purchaseDetail` | 6 | 增删改查、Excel 导入导出 |
| **合计** | — | **41** | — |

### 6.2 接口详细列表

#### 6.2.1 用户管理 `/user`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/user/list` | — | 查询所有用户 |
| GET | `/user/checkHasEmployees` | — | 检查员工表是否为空 |
| GET | `/user/getById` | `?id=` | 根据 ID 查询用户 |
| POST | `/user/login` | `?username=&password=` | 用户登录 |
| POST | `/user/register` | JSON Body | 用户注册 |
| POST | `/user/add` | JSON Body | 新增用户 |
| POST | `/user/update` | JSON Body | 修改用户 |
| GET | `/user/delete` | `?id=` | 删除用户 |
| POST | `/user/import` | Multipart File | Excel 导入用户 |
| GET | `/user/export` | — | Excel 导出用户（blob） |

#### 6.2.2 供应商管理 `/supplier`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/supplier/list` | — | 查询所有供应商 |
| POST | `/supplier/add` | JSON Body | 新增供应商 |
| POST | `/supplier/update` | JSON Body | 修改供应商 |
| GET | `/supplier/delete` | `?id=` | 删除供应商 |
| POST | `/supplier/import` | Multipart File | Excel 导入供应商 |
| GET | `/supplier/export` | — | Excel 导出供应商（blob） |

#### 6.2.3 商品管理 `/goods`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/goods/list` | — | 查询所有商品（含供应商名） |
| POST | `/goods/add` | JSON Body | 新增商品 |
| POST | `/goods/update` | JSON Body | 修改商品 |
| GET | `/goods/delete` | `?id=` | 删除商品 |
| POST | `/goods/import` | Multipart File | Excel 导入商品 |
| GET | `/goods/export` | — | Excel 导出商品（blob） |

#### 6.2.4 会员管理 `/member`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/member/list` | — | 查询所有会员 |
| GET | `/member/getById` | `?id=` | 根据 ID 查询会员 |
| POST | `/member/add` | JSON Body | 新增会员 |
| POST | `/member/update` | JSON Body | 修改会员 |
| GET | `/member/delete` | `?id=` | 删除会员 |
| GET | `/member/export` | — | Excel 导出会员（blob） |

#### 6.2.5 采购主表 `/purchaseMain`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/purchaseMain/list` | — | 查询所有采购主表 |
| POST | `/purchaseMain/add` | JSON Body | 新增采购单 |
| POST | `/purchaseMain/update` | JSON Body | 修改采购单 |
| GET | `/purchaseMain/delete` | `?id=` | 删除采购单 |
| POST | `/purchaseMain/import` | Multipart File | Excel 导入采购主表 |
| GET | `/purchaseMain/export` | — | Excel 导出采购主表（blob） |

#### 6.2.6 采购明细 `/purchaseDetail`

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/purchaseDetail/list` | — | 查询所有采购明细 |
| POST | `/purchaseDetail/add` | JSON Body | 新增明细 |
| POST | `/purchaseDetail/update` | JSON Body | 修改明细 |
| GET | `/purchaseDetail/delete` | `?id=` | 删除明细 |
| POST | `/purchaseDetail/import` | Multipart File | Excel 导入采购明细 |
| GET | `/purchaseDetail/export` | — | Excel 导出采购明细（blob） |

---

## 7. 权限管理设计

### 7.1 管理员分层权限模型

```
                    ┌──────────────────┐
                    │   一号管理员 (11)  │
                    │   全部管理权限     │
                    └────────┬─────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                                     ▼
┌──────────────────┐                 ┌──────────────────┐
│  二号管理员 (10)  │                 │  三号管理员 (01)  │
│  供应商管理 ✅    │                 │  员工管理 ✅      │
│  商品管理 ✅      │                 │  采购管理 ✅      │
│  会员管理 ✅      │                 │  会员管理 ✅      │
│  员工管理 👁️     │                 │  供应商管理 👁️   │
│  采购管理 👁️     │                 │  商品管理 👁️     │
└──────────────────┘                 └──────────────────┘
```

### 7.2 前端权限控制

在 [Layout.vue](frontend/src/views/Layout.vue) 中通过计算属性控制菜单项的显示：

```javascript
const canManageSupplier  = computed(() => role === '1' && (adminLevel === 1 || adminLevel === 2))
const canManageGoods     = computed(() => role === '1' && (adminLevel === 1 || adminLevel === 2))
const canManageEmployee  = computed(() => role === '1' && (adminLevel === 1 || adminLevel === 3))
const canManagePurchase  = computed(() => role === '1' && (adminLevel === 1 || adminLevel === 3))
```

各管理页面中通过 `hasManagePermission` 控制操作按钮（新增、编辑、删除、导入）的显示。

### 7.3 后端权限控制

后端在 `SysUserService` 中提供权限判断方法，各 Controller 可调用这些方法进行后端权限校验（当前版本未在 Controller 层强制校验，但提供了完整的判断逻辑）。

---

## 8. 用户认证与注册流程

### 8.1 登录流程

```
用户打开系统
  │
  ├─ 员工表为空？
  │   └─ 是 → 提示并退出到登录页
  │
  ├─ 选择角色（管理员/普通用户）
  │
  ├─ 输入账号密码
  │   ├─ 管理员账号: 自动截断为 6 位
  │   └─ 普通用户: 自动截断为 9 位
  │
  ├─ 点击登录
  │   └─ POST /user/login
  │       ├─ 成功 → 识别管理员级别 → 存入 localStorage → 跳转主页
  │       └─ 失败 → 提示 "用户名或密码错误"
  │
  └─ 进入主页
      └─ 普通用户 → 检查员工表是否为空 → 为空则强制退出
```

### 8.2 注册流程

```
用户选择注册选项卡
  │
  ├─ 选择角色
  │
  ├─ 输入账号
  │   ├─ 管理员: 必须以 11/10/01 开头，恰好 6 位
  │   └─ 普通用户: 必须以 00 开头，恰好 9 位
  │
  ├─ 输入密码 (实时强度检测)
  │   ├─ 至少 8 位
  │   ├─ 包含大写字母
  │   ├─ 包含小写字母
  │   ├─ 包含数字
  │   └─ 包含特殊字符(@$!%*?&)
  │
  ├─ 选择区号 + 输入手机号
  │   └─ 根据区号验证号码格式
  │
  ├─ 点击注册
  │   └─ POST /user/register
  │       ├─ 管理员: 检查是否已有该前缀管理员
  │       ├─ 普通用户: 检查员工表是否存在预录入记录
  │       ├─ 密码全局唯一性检查
  │       ├─ 电话同区号唯一性检查
  │       ├─ 成功 → 提示并切换到登录选项卡
  │       └─ 失败 → 提示具体错误信息
  │
  └─ 管理员需提前在员工表中录入员工信息，员工才能注册
```

### 8.3 退出登录流程

```
用户点击退出
  │
  ├─ 一号管理员或三号管理员？
  │   └─ 是 → 弹窗询问 "是否有新的员工需要录入？"
  │       ├─ 确定 → 跳转到员工管理页面
  │       └─ 取消 → 清除 localStorage → 跳转登录页
  │
  └─ 二号管理员或普通用户？
      └─ 直接清除 localStorage → 跳转登录页
```

---

## 9. Excel 导入导出设计

### 9.1 技术实现

使用 Apache POI 5.2.5 进行 Excel 文件的读写操作。

- **导入**: 读取 `.xlsx` 文件的第一页数据（跳过表头行），按列映射到实体字段
- **导出**: 创建 `.xlsx` 文件，设置表头样式（加粗居中），逐行写入数据，自动调整列宽

### 9.2 ExcelUtil 核心方法

```java
// 位置: backend/.../util/ExcelUtil.java

// 导入: 读取 Excel 输入流，根据表头数组映射列名
public static List<Map<String, Object>> importExcel(
    InputStream inputStream, String[] headers
) throws Exception { ... }

// 导出: 根据表头数组和字段名数组，通过反射获取数据并写入 Excel
public static byte[] exportExcel(
    String[] headers, String[] fieldNames, List<?> dataList
) throws Exception { ... }
```

### 9.3 各模块导入字段映射

| 模块 | 导入字段 |
|------|----------|
| **供应商** | supplierCode, supplierName, shortName, address, phone, email, contactPerson, contactPhone, remark |
| **商品** | goodsCode, goodsName, price, supplierId, intro, remark |
| **员工** | username, password, realName, phone, salary, role, remark |
| **采购主表** | purchaseNo, userId, totalNum, totalPrice, purchaseTime, remark |
| **采购明细** | detailNo, purchaseNo, goodsId, goodsNum, goodsPrice, totalPrice, remark |

### 9.4 导出文件命名

| 模块 | 文件名 |
|------|--------|
| 供应商 | `供应商数据.xlsx` |
| 商品 | `商品数据.xlsx` |
| 员工 | `员工数据.xlsx` |
| 会员 | `会员数据.xlsx` |
| 采购主表 | `采购主表数据.xlsx` |
| 采购明细 | `采购明细数据.xlsx` |

---

## 10. 国际化电话号码设计

### 10.1 存储格式

电话号码统一使用 `区号|号码` 格式存储：

```
示例: +86|18047651111
      +1|2125551234
      +852|91234567
```

### 10.2 支持的国家/地区

系统支持 29 个国家/地区的电话号码验证：

| 地区 | 区号 | 位数 | 首位规则 |
|------|------|:---:|------|
| 中国大陆 | +86 | 11 | 1 开头 |
| 中国香港 | +852 | 8 | 5/6/9 开头 |
| 中国澳门 | +853 | 8 | 6 开头 |
| 中国台湾 | +886 | 10 | 09 开头 |
| 日本 | +81 | 10-11 | — |
| 韩国 | +82 | 10-11 | — |
| 新加坡 | +65 | 8 | — |
| 泰国 | +66 | 10 | — |
| 马来西亚 | +60 | 10 | — |
| 越南 | +84 | 10 | — |
| 印度 | +91 | 10 | — |
| 阿联酋 | +971 | 9 | — |
| 沙特 | +966 | 9 | — |
| 印尼 | +62 | 10-12 | — |
| 菲律宾 | +63 | 10 | — |
| 美国/加拿大 | +1 | 10 | — |
| 俄罗斯 | +7 | 10 | — |
| 英国 | +44 | 11 | — |
| 德国 | +49 | 10-11 | — |
| 法国 | +33 | 9 | — |
| 意大利 | +39 | 10 | — |
| 西班牙 | +34 | 9 | — |
| 瑞士 | +41 | 9 | — |
| 瑞典 | +46 | 9 | — |
| 挪威 | +47 | 8 | — |
| 澳大利亚 | +61 | 9 | — |
| 新西兰 | +64 | 8-9 | — |
| 巴西 | +55 | 11 | — |
| 阿根廷 | +54 | 10 | — |

### 10.3 验证逻辑

```java
// SysUserService.validatePhone()
public Result<String> validatePhone(String areaCode, String phone) {
    // 1. 清理空格和连字符
    // 2. 根据区号匹配对应的验证规则（位数、开头数字）
    // 3. 使用正则表达式验证
    // 4. 返回验证结果
}
```

### 10.4 唯一性规则

- **同区号**: 电话号码不可重复
- **不同区号**: 电话号码可以重复（因为区号已区分）

---

## 11. 部署说明

### 11.1 环境要求

| 软件 | 版本 | 说明 |
|------|------|------|
| JDK | 21+ | Java 开发环境 |
| MySQL | 8.0+ | 数据库服务 |
| Maven | 3.9+ | 后端构建工具 |
| Node.js | 18+ | 前端构建环境（需 npm） |

### 11.2 数据库部署

1. 启动 MySQL 服务
2. 创建数据库：
   ```sql
   CREATE DATABASE IF NOT EXISTS supermarket_db
     DEFAULT CHARSET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
   ```
3. 导入数据库脚本：
   ```bash
   mysql -u root -p supermarket_db < database/_localhost-2026_06_04_20_24_31-dump.sql
   ```
4. 确保三个管理员和测试数据已导入

### 11.3 后端部署

```bash
# 1. 进入后端目录
cd backend

# 2. 确认 application.yml 中数据库连接信息正确
#    spring.datasource.url=jdbc:mysql://localhost:3306/supermarket_db
#    spring.datasource.username=root
#    spring.datasource.password=Sh241612

# 3. 使用 Maven 构建
mvn clean package -DskipTests

# 4. 运行
java -jar target/backend-0.0.1-SNAPSHOT.jar

# 或者使用 Maven 直接运行
mvn spring-boot:run
```

服务启动后默认运行在 `http://localhost:8080`

### 11.4 前端部署

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 开发模式运行
npm run dev

# 4. 生产构建
npm run build
```

开发服务器默认运行在 `http://localhost:5173`

### 11.5 CORS 配置

后端已配置 CORS 允许所有来源的跨域请求（开发环境适用）：
```java
// CorsConfig.java
registry.addMapping("/**")
    .allowedOriginPatterns("*")
    .allowedMethods("GET", "POST", "PUT", "DELETE")
    .allowCredentials(true);
```

生产环境建议限制具体的允许来源。

### 11.6 默认管理员账号

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| `110001` | `Sh123456@` | 一号管理员 | 所有权限 |
| `100001` | `Sh1234567@` | 二号管理员 | 供应商、商品、会员管理 |
| `010001` | `Sh1234567!` | 三号管理员 | 员工、采购、会员管理 |

---

## 12. 开发规范

### 12.1 Java 代码规范

- 使用 Lombok `@Data` 简化实体类
- Service 层返回 `Result<T>` 统一格式
- Mapper 接口使用 `@Mapper` 注解
- XML 映射文件放置在 `resources/mapper/` 目录
- 遵循驼峰命名和下划线自动映射

### 12.2 Vue 代码规范

- 使用 `<script setup>` 语法（Composition API）
- 组件文件使用 PascalCase 命名
- API 调用封装在 `api/` 目录各模块中
- 页面组件放在 `views/` 目录

### 12.3 数据库规范

- 表名和列名使用小写 + 下划线（snake_case）
- 主键使用 `id BIGINT AUTO_INCREMENT`
- 关联字段使用 `关联表_id` 命名（如 `supplier_id`）
- 所有表使用 InnoDB 引擎
- 字符集使用 utf8mb4

### 12.4 Git 规范

- 每天提交代码到 Git
- 提交信息使用中文简要描述变更内容
- 仓库包含 4 个文件夹：`database/`、`backend/`、`frontend/`、`log/`

---

## 13. 安全设计

### 13.1 已实现的安全措施

| 措施 | 说明 |
|------|------|
| CORS 配置 | 已配置跨域请求规则 |
| 输入长度限制 | 管理员 6 位、普通用户 9 位自动截断 |
| 密码复杂度验证 | 8+ 位、大小写字母、数字、特殊字符 |
| 密码唯一性 | 所有用户密码不能重复 |
| 电话格式验证 | 根据 29 个区号分别验证 |
| 账号唯一限制 | 管理员最多 3 人、前缀不可重复 |
| SQL 注入防护 | MyBatis 参数化查询 |
| 前后端双重校验 | 前端实时验证 + 后端服务端验证 |

### 13.2 已知安全风险与建议

| 风险 | 严重程度 | 建议方案 |
|------|:---:|------|
| **密码明文存储** | 🔴 高 | 使用 BCrypt 或 Argon2 哈希存储 |
| **HTTP 明文传输** | 🟡 中 | 生产环境启用 HTTPS |
| **无 Token/Session 机制** | 🟡 中 | 引入 JWT 或 Spring Security |
| **无请求频率限制** | 🟡 中 | 添加限流中间件 |
| **无操作审计日志** | 🟢 低 | 增加操作日志记录表 |
| **敏感数据未加密** | 🟢 低 | 手机号等字段可考虑加密 |

---

## 14. 未来改进方向

### 14.1 功能增强

1. **进销存完整闭环**: 增加销售管理模块、库存管理模块
2. **报表统计**: 增加数据统计图表（销售趋势、采购成本分析等）
3. **通知提醒**: 库存不足告警、采购到期提醒
4. **批量操作**: 支持多选批量删除、批量修改
5. **数据导入模板**: 提供标准 Excel 模板下载

### 14.2 技术升级

1. **安全升级**: 引入 Spring Security + JWT 实现认证授权
2. **密码加密**: 使用 BCryptPasswordEncoder
3. **HTTPS**: 配置 SSL 证书
4. **Redis 缓存**: 提升查询性能、实现分布式 Session
5. **分布式锁**: 防止并发数据冲突
6. **接口文档**: 集成 Swagger / Knife4j
7. **单元测试**: 增加 Service 层和 Controller 层测试覆盖
8. **日志系统**: 集成 Logback/ELK 完善日志记录
9. **CI/CD**: 配置自动化构建和部署流水线

### 14.3 项目结构优化

1. 引入 DTO/VO 层分离数据传输对象
2. 添加全局异常处理 (`@ControllerAdvice`)
3. 统一参数校验 (`@Valid` + JSR-303)
4. 引入 MyBatis-Plus 简化单表 CRUD

---

## 附录 A：技术依赖清单

### 后端 Maven 依赖

| GroupId | ArtifactId | Version | 说明 |
|---------|-----------|---------|------|
| org.springframework.boot | spring-boot-starter-webmvc | 4.0.6 | Spring Web MVC |
| org.mybatis.spring.boot | mybatis-spring-boot-starter | 4.0.1 | MyBatis 集成 |
| com.mysql | mysql-connector-j | (runtime) | MySQL JDBC 驱动 |
| org.projectlombok | lombok | (optional) | 代码简化 |
| org.apache.poi | poi-ooxml | 5.2.5 | Excel 读写 |

### 前端 NPM 依赖

| 包名 | 版本 | 说明 |
|------|------|------|
| vue | ^3.5.34 | Vue 3 框架 |
| element-plus | ^2.14.1 | Element Plus UI 组件库 |
| @element-plus/icons-vue | ^2.3.2 | Element Plus 图标库 |
| vite | ^8.0.12 | 构建工具 |
| @vitejs/plugin-vue | ^6.0.6 | Vite Vue 插件 |

---

## 附录 B：文件清单

### 后端文件（17 个 Java + 6 个 XML）

```
backend/src/main/java/com/supermarket/backend/
├── BackendApplication.java
├── common/
│   └── Result.java
├── config/
│   └── CorsConfig.java
├── controller/
│   ├── GoodsController.java
│   ├── MemberController.java
│   ├── PurchaseDetailController.java
│   ├── PurchaseMainController.java
│   ├── SupplierController.java
│   └── SysUserController.java
├── entity/
│   ├── Goods.java
│   ├── Member.java
│   ├── PurchaseDetail.java
│   ├── PurchaseMain.java
│   ├── Supplier.java
│   └── SysUser.java
├── mapper/
│   ├── GoodsMapper.java
│   ├── MemberMapper.java
│   ├── PurchaseDetailMapper.java
│   ├── PurchaseMainMapper.java
│   ├── SupplierMapper.java
│   └── SysUserMapper.java
├── service/
│   ├── GoodsService.java
│   ├── MemberService.java
│   ├── PurchaseDetailService.java
│   ├── PurchaseMainService.java
│   ├── SupplierService.java
│   └── SysUserService.java
└── util/
    └── ExcelUtil.java
```

### 前端文件（17 个源文件）

```
frontend/src/
├── main.js
├── App.vue
├── style.css
├── router/
│   └── index.js
├── utils/
│   └── request.js
├── api/
│   ├── goods.js
│   ├── member.js
│   ├── purchaseDetail.js
│   ├── purchaseMain.js
│   ├── supplier.js
│   └── user.js
└── views/
    ├── Employee.vue
    ├── Goods.vue
    ├── Layout.vue
    ├── Login.vue
    ├── Member.vue
    ├── Purchase.vue
    ├── Supplier.vue
    └── UserInfo.vue
```

---

## 附录 C：版本记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.1 | 2026-06-05 | 完善采购模块设计文档：新增 §3.3 采购模块详细设计、§4.4.3 采购业务逻辑、§5.5 采购管理页面设计；增强 PurchaseMain/PurchaseDetail 实体描述 | Sun Hao |
| v1.0 | 2026-06-05 | 初始版本，完成所有核心功能模块 | Sun Hao |
