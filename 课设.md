# 1\. 超市进销存管理系统


系统分为两个权限部分：管理员用户登录模块，普通用户登陆模块。
(注册，登录界面是一个整个的小弹窗，分为两个部分左侧是小动画frontend\public\cat_happy.mp4，
右侧是注册登录窗口选项，管理员和普通用户，还要考虑到之后与后端链接将用户信息保存等......)

管理员用户登录模块包括：供应商管理，员工管理，商品管理，会员管理，采购管理。
普通用户登陆模块包括：个人信息，商品信息，采购信息。


本项目实现以下功能：

（1）管理员用户登录模块：

A、供应商管理（考虑当外部提供有供应商数据的文件或者execl表时，管理员需要对其文件或execl表进行检查，确认无误后，直接进行导入，不用管理员一个一个录入，减轻工作量）
批量录入供应商数据，手动输入供应商编号，供应商名称，供应商简称，地址，公司电话，邮件，联系人，联系人电话，备注。
查询并列出表中所有的供应商信息。（支持导出成文档，execl表，文件等形式）
修改指定供应商的信息。
删除指定供应商数据。

B、商品管理（考虑当外部提供有商品数据的文件或者execl表时，管理员需要对其文件或execl表进行检查，确认无误后，直接进行导入，不用管理员一个一个录入，减轻工作量）
批量录入商品数据，手动输入商品编号，商品名称，商品单价，供应商编号，商品简介，备注等。
查询并列出所有商品信息。（支持导出成文档，execl表，文件等形式）
修改制定商品的商品信息。
删除指定商品数据。

C、员工管理（考虑当外部提供有员工数据的文件或者execl表时，管理员需要对其文件或execl表进行检查，确认无误后，直接进行导入，不用管理员一个一个录入，减轻工作量）
批量录入员工数据，手动输入员工编号，员工姓名，员工密码，员工级别，员工电话，员工工资，备注等。
查询并列出所有员工信息。（支持导出成文档，execl表，文件等形式）
修改指定员工信息。
删除指定员工数据。

D、采购管理（考虑当外部提供有主表，明细数据的文件或者execl表时，管理员需要对其文件或execl表进行检查，确认无误后，直接进行导入，不用管理员一个一个录入，减轻工作量）
录入采购主表数据，手动输入采购清单号，员工编号（外码），采购数量，采购总价，采购时间，备注。
录入采购明细数据，手动输入采购明细号，采购清单号（外码），商品编号（外码），采购数量，商品单价，商品总价，备注。
查询并列出所有采购主表和采购明细信息。（支持导出成文档，execl表，文件等形式）
修改指定采购主表信息和采购明细信息。
删除指定采购主表信息和采购明细信息。

（2）普通用户登陆模块：

A、查询自己的员工信息。
B、查询所有商品信息。（支持导出成文档，execl表，文件等形式）
C、查询所有的采购信息。（支持导出成文档，execl表，文件等形式）

技术要求：后端采用Spring Boot+MyBatis，前端采用Vue

加分项包括：（1）事务管理；（2）redis；（3）分布式锁；（4）https；（5）多线程；（6）敏感数据加解密


要求:从课设第一天开始，需要把每天写的代码每天上传到git中，日志，是写markdown的文档，创建git仓库，版本库 4个文件夹

1 数据库  2 后端服务代码  3前端代码 4 log。 最外面的readme.md是项目整体的说明和部署说明

！！！登陆页面

系统只有3个管理员，3个管理员账号限制长度为6位，（只保留输入的前6位）开头分别是11，10，01，员工表里如果三个管理员都已经注册则不允许在有多余的管理员注册，

一号管理员，11开头的管理员有最高权限，A、供应商管理，B、商品管理，C、员工管理，D、采购管理，会员管理，
二号管理员，10开头的管理员有部分权限，只能管理A、供应商管理，B、商品管理，会员管理，（能够查看C、员工管理，D、采购管理，不能修改）
三号管理员，01开头的管理员有部分权限，只能管理C、员工管理，D、采购管理，会员管理，（能够查看A、供应商管理，B、商品管理，不能修改）

每个管理员登陆进入系统后，在每个管理页面，显示其当前的身份，是几号管理员。
在一号和三号管理员退出时弹出提示，是否有新的员工，有就跳转到员工表等待管理员录入，没有就退出，二号管理员不用弹出提示。

普通用户注册时，先查询员工表，若员工表里没有任何信息或者只有管理员时，则自动退出到登陆页面，提示员工“系统显示管理员还未录入员工信息，该用户为非法用户，不能注册，请联系管理员或者等待管理员录入员工信息后重试”。

普通员工账号长度为9位（只保留输入的前9位）必须是00开头，否则不能注册并提示。

普通员工必须要根据管理员（一号或者三号）在员工表里填入员工的信息注册，登录，员工表里没有员工信息则没有员工，员工不能注册登录


密码要显示强度建议，至少8位，至少包含一个大写或小写字母，数字，一个特殊字符，所有用户密码不能重复，否则不能注册。


只要是涉及电话号码填写（除了公司电话以外）都要按照以下规则填写：
注册时电话号码这一栏前面要有类似于“+86”这样的选择框(选择框里有区号和对应的地区名)用来表示区号（区号用蓝色字体），如果选择的地区不一样，电话号码可以重复，如果选择的地区一样，电话号码不能重复，若重复则提示。
（默认选择“+86”——中国大陆）电话号必须根据提示的长度，开头数字，来填写，否则不能注册。

在表里显示电话号码时，区号后面跟着手机号，并且用竖杠分隔。

地区	  国际区号	    手机号位数	     备注

中国大陆	+86	        11 位	        1 开头

中国香港	+852	    8 位	        5/6/9 开头

中国澳门	+853	    8 位	        6 开头

中国台湾	+886	    10 位	        09 开头

日本	    +81	        10～11 位

韩国	    +82	        10～11 位

新加坡	    +65	        8 位

泰国	    +66	        10 位

马来西亚	+60	        10 位

越南	    +84	        10 位

印度	    +91	        10 位

阿联酋	    +971	    9 位

沙特	    +966	    9 位

印尼	    +62	        10～12 位   

菲律宾	    +63	        10 位

美国 / 加拿大	+1	        10 位	北美统一 10 位

俄罗斯	        +7	        10 位

英国	        +44	        11 位

德国	        +49	        10～11 位

法国	        +33	        9 位

意大利	        +39	        10 位

西班牙	        +34	        9 位

瑞士	        +41	        9 位

瑞典	        +46	        9 位

挪威	        +47	        8 位

澳大利亚	    +61	        9 位    

新西兰	        +64	        8～9 位

巴西	        +55	        11 位

阿根廷	        +54	        10 位


如果涉及时间填写，用选项框选择时间，格式为“年-月-日”。
只有会员管理的注册时间精确到小时，其他时间都为“年-月-日”。
数据库自动生成的时间则显示为“年-月-日 时:分:秒”。

登录模块，当填写注册信息但没有填写完，因为某些事情不在注册模块，过一会后又回到注册页面时，应该清空注册信息从新填写。当填写登录信息但没有填写完，因为某些事情不在登录模块，过一会后又回到登录页面时，应该清空登录信息从新填写

登录注册页面还要新增密码找回功能，用户可以在登录页面点击忘记密码，输入手机号，系统会发送验证码到手机号，用户输入验证码后，可以重置密码。

验证码的时间限制为一分钟，超过一分钟没有输入验证码，验证码就会过期。
当用户正常登录时输完编号，密码后要加一个人机验证。
用户忘记密码时，输入完手机号后要加一个人机验证。

将log日志中image文件夹中的图片放入对应的md文件中，并根据今天的工作完成今天的日志文档要含有图片描述。今天是第三天，md文档命名格式仿照前几天的。

！！！会员管理

在会员表里增加一个属性：会员等级，会员等级有SVIP、VIP、普通会员三种
11管理员可以修改除了自己以外的会员等级
10管理员可以修改除了11，自己和01管理员以外的会员等级
01管理员可以修改除了11，10和自己管理员以外的会员等级

管理员注册后最高级管理员自动加入会员表并设置为SVIP，另外两个管理员注册后自动加入会员表并设置为VIP,其他人成为会员需要管理员添加其信息。

员工的会员等级在员工表里由管理员添加新员工时编辑，当然点击编辑时也可以编辑
员工注册后显示在会员表，个人信息
管理员没有给员工会员身份则不能默认为普通会员，会员表里不能有，员工个人信息会员等级哪里显示无
普通用户的会员等级需要在等级前面加上员工两个字用于区分


测试时我发现我用员工表里的电话号去会员表里注册，换了一个人名，也可以注册成功这是不对的，违反了（如果选择的地区一样，电话号码不能重复）这一原则。

若是以前这个员工有会员等级但我编辑了该员工的会员等级为无保存后那会员表里应该就没有这个会员了
相反若是以前这个员工没有会员等级但我编辑了该员工的会员等级使他为有保存后那会员表里应该就有这个会员了

注意检查会员表里不能出现员工表里管理员新增，编辑或导入的但是还没有注册的员工

！！！员工管理

在员工管理点击新增员工时，
如果当前身份是管理员，只能增加普通用户，即使表里管理员小于3个，也不能增加管理员，管理员只能靠自己去注册。
当前身份是普通用户，那当前都没有增加员工这个功能只有
A、查询自己的员工信息。
B、查询所有商品信息。（支持导出成文档，execl表，文件等形式）
C、查询所有的采购信息。（支持导出成文档，execl表，文件等形式）

无论管理员还是普通用户，页面右上角都应该有一个圆形的头像，支持点击显示。支持上传图片作为图像，显示在头像里。支持编辑当前登录的用户的信息如性别，年龄，住址等。
这样员工就只用在左侧就可以省去查询员工表的步骤。

当然，管理员本人的员工编号、会员等级不能修改、员工密码修改要进行二次确认。普通用户的员工编号、会员等级不能修改、员工密码修改要进行二次确认。

管理员在员工管理页面中点击所有人姓名那一栏时，应该显示出员工的所有信息。把姓名用下划线标记一下方便用户知道这里可以点击

给每个功能增加翻页功能，一页最多纪录10个数据项，点击下一页，显示下一页的数据项。

管理员新增员工时不要设置姓名，密码，电话号。这几项在员工表留空，等员工根据表中员工编号注册时员工自己填写姓名，密码，电话号，登陆后才能显示给管理员。

无论是管理员个人信息还是员工个人信息，年龄都要有一个限制，公司不招收年龄大于66岁小于18岁的员工，上线设置为66岁，下线设置为18岁。
当用户编辑个人信息保存时，如果用户输入的年龄不在这个范围内时，应该提示用户年龄不符合公司规定，请重新输入。

11管理员和01管理员在员工管理页面中点击编辑时只有11管理员可以编辑所有员工，01管理员只能编辑普通用户信息。

每个模块的页面显示的当前编号应该根据当前表里的数据项依次排列

！！！采购管理

在采购管理页面中点击采购编号，应该显示采购信息
在采购管理页面新增采购单时，选择员工时不应该显示管理员新增但是员工还未注册的员工
我在采购管理新增采购订单时编辑完点击确定提示我请求失败

先录入采购主表数据，手动输入采购清单号，选择员工编号（外码），选择商品，输入采购数量，输入采购总价，采购时间，备注。
再录入采购明细数据，手动输入采购明细号，选择主表里已经有的采购清单号（外码），选择商品编号（外码），输入采购数量，输入商品单价，计算商品总价，备注。
当采购主表的数据为空时禁止新增采购细节表并给出提示。

采购总价在采购主表里需要手动输入，在细节表里自动计算商品总价，采购总价和商品总价可能不一样。
采购明细号从PD1开始依次排序。

采购管理注意检查
在采购主表里没有该清单号时采购明细表不能新增导入编辑该清单号，给出提示

！！！Skills配置

我现在想让你增加一些技能skills 
我给你地址你自己部署添加 
这些技能不单单只用于这一个项目，而是以后所有项目都可以用
我希望每次开发项目遇到问题你都能自动调用这些相应技能


前端设计
github.com/anthropics/skills/tree/main/skills/frotend-design
自己创建skills
github.com/anthropics/skills/tree/main/skills/skill-creator
联网搜索
github.com/eze-is/web-access
pua检查
github.com/tanweai/pua
PDF文档能力
github.com/anthropics/skills/tree/main/skills/pdf

Superpowers
/install superpowers

OpenSpec
/install openspec

Gstack
git clone --single-branch --depth 1 https://github.com/garrytan/gstack.git ~/.claude/skills/gstack && cd ~/.claude/skills/gstack && ./setup
将 gstack section 添加到
CLAUDE.md,
列出所有可用skill: /office-hours,/
plan-ceo-review,/plan-eng-review,/
plan-design-review,/review, /ship,/qa,/cso,/
autoplan,/retro,
/investigate, /browse等，并要求所有网页浏览使
用/browse skill

自己为项目创建一个skill，用于处理项目中的问题。

！！！高级优化

我想要在项目中使用多线程，来提高系统的并发性能。
我想要在项目中使用redis+分布式锁，来解决并发问题。
我还想要https加密，来保护用户数据的安全，同时能够支持我将网址发给多名用户，多名用户可以同时登录使用。
我想要在项目中使用事务管理，来确保数据的一致性。
我想要在项目使用缓存，来提高系统的性能。

用户现在是可以登录进去了，但是特别特别慢要等好久才可以显示出页面而且会提示网络延时还会卡退，登录小动画也显示不出来

能不能调整一下界面不仅适配PC端，还要适配手机端的页面

当有多个用户使用cpolar生成的网址登录时会非常卡而且会弹出警告
用户使用cpolar生成的网址页面加载的非常慢，即使是在本地我用http://localhost:5173/登录的速度远远高于用cpolar生成的网址登录的速度。

对于侧边选项栏我想让它平时隐藏鼠标悬浮到哪里时在显现出来选择
你能不能根据我的背景图设计一个带有动态效果的背景图
背景只有颜色太单调，根据这个画面web-background设计一个动态的背景
侧边栏文字看不清调整一下
有粒子效果了，但是一点都不明显，因为背景是暖色调，粒子和背景一个颜色就看不出来了，你思考设计一下既不打乱画面和谐又能看到动态效果
有是有了但是效果非常不明显

合理添加搜索栏

现在网页的显示栏是紫色的闪电加上frontend 的logo，我想要改成一个超市加上SuperMarket 的logo

！！！智能体
github是开源的不要上传

我想为我的项目添加一个智能体
在登陆后可以看见悬浮的一个白色小猫智能体，可以和它进行对话
比如询问一些进销存的问题：1、如何添加员工？2、如何添加会员？3、如何添加商品？4、如何添加采购单？5、如何添加销售单？6、如何添加销售退货单？7、如何添加采购退货单？8、如何添加仓库？9、如何添加仓库管理员？10、如何添加仓库管理员权限？11、如何添加仓库管理员权限？12、如何添加仓库管理员权限？13、如何添加仓库管理员权限？14、如何添加仓库管理员权限？15、如何添加仓库管理员权限？16、如何添加仓库管理员权限？等等......

白色小猫参考public文件夹中的cat-agent.mp4

请从设计到实现先进行严谨的设计写出md设计文档再一步一步进行实现

用户有可能提问一些其他没有出现过但是合理的问题，智能体有可能回答的不合理，能不能想个办法啊彻底解决用户有可能提问一些其他没有出现过但是合理的问题，智能体有可能回答的不合理，能不能想个办法啊彻底解决

！！！问题
会员管理时新增会员我选择了日期时间但是我点击确定后自动更改为了当前时间

商品编号重复应该给出提示现在商品编号重复，系统直接报错了

excel去掉了id但是商品编号，员工编号，会员编号，供应商编号这些在对应的excel导出表都要有并且是按照表里的编号不是id
导出的excel表第一行汉字还是看不完整，在加宽一些

我编辑完excel表导入时显示导入失败
员工表excel导入员工时失败，在员工管理模块用excel导入时注意限制条件检查，只有员工信息可以导入，管理员信息不可以导入

会员管理也支持导入excel

我导入excel表时我在表中写了时间但是导入后时间变成了当天时间